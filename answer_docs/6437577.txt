<p>and is the default action in a regexp, i.e. first match this then followed by that and so on.</p>

<p>to match cat AND dog, use something like </p>

<pre><code>cat.*dog
</code></pre>

<p>which means, match "cat" followed by anything AND then followed by "dog". But then again I might have misunderstood your question...</p>