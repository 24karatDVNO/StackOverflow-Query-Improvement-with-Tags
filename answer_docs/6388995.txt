<p>You can start an AsyncTask (or a Timer and Timertask, or just a Thread) that delays (using <code>Thread.sleep</code>) and then fires the notification. You don't want to sleep on the UI thread, which is why you need a separate thread of some sort.</p>