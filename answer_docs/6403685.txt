<p>Now, i'm just guessing here since the rest of the implementation isn't shown, but I'm assuming you are using those derived measurements as <code>px</code></p>

<p>Take a look at <code>dp</code>.   That essentially does all those things you did, automatically, for any device.  (Device Independent Pixel)</p>