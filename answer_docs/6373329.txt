<p>I would use <a href="http://php.net/strip_tags" rel="nofollow">strip_tags</a> and <a href="http://php.net/htmlentities" rel="nofollow">htmlentities</a>, then place any links within an <code>&lt;a&gt;</code> using preg_replace.</p>