<p>The difference is that typing into a Label causes it to render the HTML from the server side while typing text into the HTML does not.</p>

<p>This is very useful if you want to change the text dynamically or if you need to deal with changing the text for internationalization.</p>