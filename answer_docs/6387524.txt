<p>In the HTML view of your post, just wrap your code in the [php] and [/PHP] tags, and it will auto-format it. Because you're using special brackets ("[" and "]") and not the standard brackets, it allows the highlighter to see them and format properly.</p>