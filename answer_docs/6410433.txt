<p>Yes, he is being bannable. Ajax is also transfering referer, so with banned referer you would be being unable to access that script. However! You can use any proxy script on free host. LIke this</p>

<pre><code>&lt;?php
echo file_get_contents("http://url-of-site-to-retreive-data.com/json.json");
?&gt;
</code></pre>