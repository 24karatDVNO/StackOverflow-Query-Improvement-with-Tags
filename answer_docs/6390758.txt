<p>Try reading Python pickle (Python's serialization) tutorials for the basic knowledge:</p>

<p><a href="http://docs.python.org/library/pickle.html" rel="nofollow">http://docs.python.org/library/pickle.html</a></p>

<p>Network transmission is a separate issue and can be dealt without serialization. Basically sending a serialized object or a file over TCP/IP connection does not differ.</p>