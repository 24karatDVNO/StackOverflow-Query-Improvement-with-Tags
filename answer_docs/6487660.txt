<p>This might be a good usage for something like <a href="http://thrift.apache.org/" rel="nofollow">thrift</a>. You can invoke methods of the python script which runs as a server. No need to resort to capturing stdout, you can just have the python script return a string to the caller.</p>