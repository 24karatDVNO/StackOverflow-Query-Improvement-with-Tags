<p>You used at the end <code>\z</code> try to use <code>\Z</code> according to <a href="http://www.regular-expressions.info/ruby.html" rel="nofollow">http://www.regular-expressions.info/ruby.html</a> it has to be a uppercase Z to match the end of the string.</p>

<p>Otherwise try to use <code>^</code> and <code>$</code> (matching the start and the end of a row) this worked for me <a href="http://regexr.com?2u1vg" rel="nofollow">here on Regexr</a> </p>