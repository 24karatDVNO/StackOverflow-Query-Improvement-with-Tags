<p>give name to the input text(name should be exact same as it is in bean)</p>

<pre><code>&lt;h:inputText name="{user.name}" value="#{user.name}" onmouseover="$(this).val();"/&gt; 

in bean-the variable name should be-(i am just giving an example) 
private name;
</code></pre>