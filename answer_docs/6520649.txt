<p>In this case there's no difference between what your two pieces of code will do.  </p>

<p>Blocks are there for use alongside other JS constructs such as functions, if, looping, etc.  For example when used with if, they mean "If a condition is met, then execute all the code in the following block".  Without the block you can only execute a single line of code based on the result of the if statement. </p>