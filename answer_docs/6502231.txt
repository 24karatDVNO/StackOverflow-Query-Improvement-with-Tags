<p>In addition to <code>nextafter()</code>, there are also <a href="http://publib.boulder.ibm.com/infocenter/zvm/v6r1/index.jsp?topic=/com.ibm.zvm.v610.edclv/frexp.htm" rel="nofollow"><code>ldexp()</code> and <code>frexp()</code></a> for dealing with mantissa and exponent explicitly.</p>