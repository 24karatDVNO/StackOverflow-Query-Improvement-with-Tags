<p>You can add a <code>final</code> modificator to your <code>MyConnection()</code> constructor's parameters, this way you can use them as parameters in the call to <code>PasswordAuthentication()</code>. Hope this helps.</p>