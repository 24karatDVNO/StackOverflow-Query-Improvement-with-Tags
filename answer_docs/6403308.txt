<p>switch statements normally are optimized to a jump table at compile time, so there is no way this could work. They have to be compile time constants. (Can you use template metaprogramming to generate them in the compile phase?)</p>