<p>Currently Corona doesn't support getting a list of files in a directory. There was a hack that used os.excute to issue a "ls" command but it turns out that only works in the Corona Simulator because the device OS is probably blocking the feature for security reasons.</p>