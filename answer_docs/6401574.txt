<p>As already said databae is creates at <code>/data/data/YOUR_PACKAGE/databases/</code> Look <a href="http://developer.android.com/reference/android/content/Context.html#getDatabasePath%28java.lang.String%29" rel="nofollow">here</a>
the <code>getDatabasePath()</code> method returns the default database path, which is very convenient.</p>