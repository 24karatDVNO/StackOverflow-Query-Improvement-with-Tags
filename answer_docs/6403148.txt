<p>I understand that you want to compare types.<br>
The best way to do this is...<br>
<code>myVar is List&lt;string&gt;</code> or<br>
<code>myVar.GetType() == myOtherVar.GetType()</code>  </p>

<p>If you don't need this... please disregard my answer.</p>