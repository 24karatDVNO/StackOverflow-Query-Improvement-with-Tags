<p>It's a bit hard to answer your question without more info. Try logging everything during application startup, and isolate the part of code which exits JVM. Also, check Java version which are used by JSW - is it the same as the one you use in Eclipse?</p>