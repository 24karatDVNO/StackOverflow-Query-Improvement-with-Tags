<p>That's because you user p.x, not c.x. The field x is not overriden by Child (as, for example, a method getX() would be). Instead, it is in the subobject, corresponding to a Child class. So, you actually have 2 x: Parent.x and Child.x.</p>