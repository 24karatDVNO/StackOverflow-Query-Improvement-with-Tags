<p>That would be possible, just store the file content as byte array. Write a converter which saves the bytes of a specified files into a text file in the format you need (so it represents an array). Then cut and paste this into your source code.</p>