<p><code>keyword.size()</code> is evaluated at runtime therefore it is not a compile time constant. Depending on the compiler it may not be allowed to use an array size that is depending on some runtime value.</p>

<p>Some further info about <a href="http://gcc.gnu.org/onlinedocs/gcc/Variable-Length.html" rel="nofollow">variable array length in gcc</a></p>