<p>This looks like a scenario for the <a href="http://en.wikipedia.org/wiki/Strategy_pattern" rel="nofollow">Strategy Pattern</a>. You could wrap the queries that retrieve certain data in strategy-objects. You can then provide a set of default strategies, but clients can provide their own strategies that use DBMS-specific stuff or use different schemata.</p>