<p>Try replacing the "and" with "][" like so:</p>

<pre><code>string xpath = "//directory[@level='" + directoryLevel+ "']/cel[@row='" + amountOfRows + "'][@column='" + amountOfColumns+ "']";
</code></pre>