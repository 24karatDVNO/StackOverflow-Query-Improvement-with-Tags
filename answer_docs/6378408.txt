<p>In addition to the checkbox list plugins NgM listed, you can also make the select element a multi select (<code>multiple=true</code>, I think), which might accomplish the same thing you're after.</p>