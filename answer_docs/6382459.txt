<p>The problem is that you have put <code>ToShortDateString()</code> at the end there, effectively converting the <code>DateTime</code> back to a <code>string</code> again. Try removing that part of the line.</p>