<p>You could try using something like a <a href="http://keith-wood.name/countdown.html" rel="nofollow">countdown plugin</a> and do a count up.</p>

<pre><code>$('#sinceCountdown').countdown({since: CUSTOMTIMEVAR, 
    format: 'HMS', description: 'Custom Time'});
</code></pre>