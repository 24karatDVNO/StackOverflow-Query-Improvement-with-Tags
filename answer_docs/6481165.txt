<p>try removing and adding a service reference through Visual Studio if you can. It´s the safest bet. WCF server and client configuration really needs to match to work. WCF service projects usually have a IMetaDateExchange behavior. Wrong server name might just be a case of a wrong endpoint entry in web.config</p>