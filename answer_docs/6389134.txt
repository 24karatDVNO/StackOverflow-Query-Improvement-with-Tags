<p>That command is part of rvm. Have you installed rvm already? <a href="https://rvm.io" rel="nofollow">https://rvm.io</a>. Follow the installation instructions carefully. It adds a lot to the login script, so just to be sure to be sure, logout and log back in to make sure it's setup properly. Skip the gemsets for now just to get up and running, then revisit that.</p>