<p>After built the dll in .net framework 3.5,if we used it in NUnit it works fine but if you want to integrate the NUnit with the Visual Studio IDE You need to change the configuration files in order to execute. But im not sure how to do that</p>