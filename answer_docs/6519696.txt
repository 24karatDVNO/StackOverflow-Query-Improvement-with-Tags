<p>Replace all you <code>&lt;</code> by <code>&amp;lt;</code> (lt = less than) and all your <code>&gt;</code> by <code>&amp;gt;</code> (gt = greater than).</p>

<p>If you're using some PHP, you can simply use <code>htmlentities()</code>, which will do that alone.</p>