<p>First, you have to know what kind of database you have to connect to. It does matter, as you won't use the same API to connect to a MySQL database, or to a SQLite one, or Oracle, or Postgres, or SQL Server ...</p>

<p>Then, have a look at the library that allows acces to the database. And make sure that the database is actually present!</p>