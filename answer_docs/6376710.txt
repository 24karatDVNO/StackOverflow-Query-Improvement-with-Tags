<p>If you are looking for a director represented by <code>*</code> you would have to enclose it in between single quotes:</p>

<pre><code>SELECT * FROM movies WHERE director='*'
</code></pre>

<p>If you're looking for any director just leave the <code>where</code> on out.</p>