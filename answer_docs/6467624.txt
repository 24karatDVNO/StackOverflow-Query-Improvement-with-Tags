<p>My suggestion would be to run both XmlDocuments through the same Xslt (doing the sorting and indenting). The resulting XmlDocuments should be easy to compare (manually) with the tool of your choice.</p>