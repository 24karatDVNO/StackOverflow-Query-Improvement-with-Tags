<p>You can use a function like <a href="http://php.net/manual/en/function.mt-rand.php" rel="nofollow"><code>mt_rand()</code></a> to generate a unique string for each row. Or just use a counter to generate a series of numbers for the rows.</p>