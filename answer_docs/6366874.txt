<p>I like to believe that the days of <code>mysql_query("SELECT * FROM table WHERE x={$_GET['val']}")</code> are over. That being said, it's OK with any decent database library as long as you use parameter binding. So go ahead and use <code>urlencode</code>.</p>