<p>Couldn't you keep an array of these counters (this would work if the number of rows is known beforehand and static)? Otherwise you could attach the counter to each <code>&lt;tr&gt;</code> element using the jquery <code>data()</code> function.</p>

<p>See: <a href="http://api.jquery.com/jQuery.data/" rel="nofollow">http://api.jquery.com/jQuery.data/</a></p>