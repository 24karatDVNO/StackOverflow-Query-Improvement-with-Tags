<p>No, the blur event won't be executed the same time a keyPress event is fired. I assume you have an <code>alert()</code> statement within your <code>checkCapsLock()</code> event handler, which causes the loss of focus.</p>