<p>I create a new project to test <code>cancelDiscovery()</code>. Once we call this method, <code>ACTION_DISCOVERY_FINISHED</code> is broadcasted. In my case, I use handler to repeat the discovery process, so I have to stop it as well.</p>