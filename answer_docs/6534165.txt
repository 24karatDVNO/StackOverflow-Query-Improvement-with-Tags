<p>Seems that debugger is already attached, in case of debugger is already attached <code>Debugger.Launch()</code> does not work. Try using <code>Debugger.Break()</code>, it will break if debugger is already attached or not.</p>