<p>User email is not one of the fields that the API returns for <code>/users</code>, see:</p>

<p><a href="https://developer.foursquare.com/docs/responses/user.html" rel="nofollow">https://developer.foursquare.com/docs/responses/user.html</a></p>

<p>Usually API's don't return emails in the clear.</p>