<p>you can save the page status inside <code>cookies</code> using <code>javascript</code> and read it again by <code>javascript</code> too.for example you can save the selected tab into a cookie and after the page has been refreshed set the selected tab id from a cookie.</p>

<p>this <a href="http://www.w3schools.com/JS/js_cookies.asp" rel="nofollow">link</a> explain how to set and retrieve cookies using javascript and this <a href="http://www.quirksmode.org/js/cookies.html" rel="nofollow">link</a></p>