<p>Using <code>synchronized</code> is to lock that particular method/code, so that the other threads will have to wait till the current thread has finished executing the synchronized block of code, meaning the synchronized code cannot be executed by more than one thread at any given instance.<br>This offers protection only within <b>your</b> application.</p>