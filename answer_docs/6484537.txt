<p>You can check out <a href="http://code.google.com/p/php5rp/" rel="nofollow">this PHP proxy that uses cURL</a>.  It's not perfect (I'm in the process of fixing it to handle PUT requests, application/json POSTS, etc).  The thing is you'll need to configure the web server to rewrite all requests to this file's file name so you can proxy them properly.</p>