<p>I think you have to url-encode the file name in the download link. The problem may be that the browser doesn't know the charset of web server's file system, so different browsers may take different assumptions about your file system's charset. Url-encoding should solve this ambiguity.</p>