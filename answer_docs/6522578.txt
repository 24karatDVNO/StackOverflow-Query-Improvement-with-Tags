<p>In your example, <code>TempData["searchParameter"]</code> is assigned to regardless of of whether <code>submitButton != "Search"</code>. Is this the behaviour you are expecting? </p>

<p>If not, an <code>else</code> statement will resolve this for you.</p>