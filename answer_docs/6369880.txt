<p>The easy way to test wide literals is to skip the formatting part of <code>printf</code>, and give your string straight to the OS: <code>WriteConsoleW(GetStdHandle(STD_OUTPUT_HANDLE), L" chinese characters:中文字", ...</code>. </p>