<p>the entire <code>IFNULL(?,xxx)</code> construct should return a value.</p>

<p>you should be able to use this value inside the <code>LIKE</code> operator just as ou are using it with the = operator.</p>