<p>Here are the options:
 1. Using Console application: Create an application which takes the sql script file as input stream, specify the range of the number which needs to be updated and provide the increment to be done. Output stream will provide the desired result. Will provide the code soon.  </p>

<ol>
<li>Using Notepad++, Excel. Copy Paste whole script in Excel. Now go to Data -> Text to column. All Id's would come in a column. Now update the cell with formula =ColumnRow + . Now merge all cells in one column. Copy/Paste script to sql manager :) ...</li>
</ol>