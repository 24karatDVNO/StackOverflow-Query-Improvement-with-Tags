<p>My personal favorite for Modal windows is the standard jQuery UI Dialog box.  It's simple, lightweight, and already available via jQuery UI, which I already run.</p>

<p>Simply build a div and call a <code>$('#id').dialog()</code></p>