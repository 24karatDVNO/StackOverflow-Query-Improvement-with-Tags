<p>Yes. This is possible. Just create a layout that contains both lists and initialize them in your activity as you normally would. If you use <code>ListActivity</code>, it will only handle one list for you. You'll need to do it the normal way for your second list.</p>