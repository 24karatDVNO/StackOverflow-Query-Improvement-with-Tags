<p>You can access related object like <code>$model-&gt;relatedModel-&gt;attribute</code>.</p>

<p>Set a break point after <code>model-&gt;findAll()</code> and look to <code>$model-&gt;_related property</code>. You must have a collection of related models there.</p>