<p>The script only runs once when the page is loaded, but jqm is caching your page, so it only runs the first time the page is retrieved - you need to bind to the "pageshow" event to run code every time the page is shown.</p>