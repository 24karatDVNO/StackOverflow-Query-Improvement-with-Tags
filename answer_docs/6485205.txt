<p>It will in general be quickest to declare <code>i</code> as an <code>int</code>. I can't imagine why you want to loop with an 8 bit integer. Perhaps you think it will be quicker than <code>int</code> but it won't.</p>