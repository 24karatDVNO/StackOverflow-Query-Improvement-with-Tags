<p>A .IDX file is a standalone index file containing one index, as opposed to .CDX which is a compound index file that can contain many. </p>

<p>Is it a permissions issue - does it work when you run it as a network administrator?</p>

<p>What version of FoxPro / Visual FoxPro is the EXE compiled in ?</p>

<p>What are the client and server OS ?</p>