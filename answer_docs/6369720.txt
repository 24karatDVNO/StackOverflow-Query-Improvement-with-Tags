<p>This works fine for me (<a href="http://jsfiddle.net/brymck/gpPEg/" rel="nofollow">jsFiddle example</a>). Any chance it's your click event handler, using ID instead of name, asking for child nodes when they're really descendant nodes (e.g. <code>&lt;div&gt;&lt;ul&gt;&lt;li&gt;&lt;input type="checkbox"&gt;&lt;/li&gt;&lt;/ul&gt;&lt;/div&gt;</code>), setting <code>status</code> to something aside from <code>"checked"</code>, <code>""</code>, <code>true</code> or <code>false</code>(which all work at least in Chrome)?</p>