<p>Quick caveat: if you're setting the header to me a bitmap, dont write HTML to the screen: (<code>echo '&lt;img src=test.jpg&gt;';</code>)</p>

<p>I would echo the header to be <code>image/jpeg</code> and then just write the raw binary to the screen.</p>