<p>every xml file needs to have a name space for it which is defined in the root tag of the xml file using the xmlns . So the suggestion by woodshy is exactly what you need ,add a : <code>xmlns:android="http://schemas.android.com/apk/res/android</code> to the first LinearLayout in your xml file. </p>