<p>with this property set (display:inline) you can't set width and height of your element. Margin, padding &amp; border is ok. I'd use floating instead, but that's because it gives me more flexibility sometimes. =]</p>