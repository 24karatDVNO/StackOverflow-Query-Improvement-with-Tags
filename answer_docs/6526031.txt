<p><code>static</code> variables are declared just like normal instance variables with the static keyword. It's illegal to declare them in a method. Also, why not just use <code>this</code> instead of assigning it to a variable?</p>