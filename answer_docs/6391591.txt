<p>it looks like your example should work, but you could try extracting the Draw method (and any others you need for polymorphism) into an Inerface, then implementing the interface in each of the classes you want in the list.</p>