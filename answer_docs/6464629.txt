<p>Is this one of those cases where <code>getElementById</code> is actually returning a form element with a <code>name</code> attribute but not an <code>id</code>..?  If so, just add an <code>id</code> attribute with the same value and try again...</p>