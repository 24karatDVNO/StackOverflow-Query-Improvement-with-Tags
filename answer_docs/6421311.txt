<p>You might notice a slower compilation time when you do wildcard import, this is because all classes would be loaded by the compiler at compile time. But it has no effect to the runtime performance.</p>