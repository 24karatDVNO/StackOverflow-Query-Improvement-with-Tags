<p>It's a sass file. Use <code>@import</code> in <code>application.css.sass</code> instead of <code>require</code>. <code>require</code> is for plain css or template engines that don't have an import system.</p>