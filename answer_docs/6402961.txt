<p>The CPU has no influence on GPU OpenCL performance.</p>

<p>You might also want to try running the OpenCL kernels on CPU. Checkout the Intel OpenCL compiler beta. You can even run kernels on both CPU and GPU.</p>