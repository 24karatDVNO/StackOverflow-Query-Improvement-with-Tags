<p>That depends on what key and value actually represent. Using Hibernate look into either <code>org.hibernate.annotations.CollectionOfElements</code> (map primitives) or <code>org.hibernate.annotations.MapKey</code> (map entities).</p>