<p>SVN only commits the files that you <code>svn add</code> first. If you did not run <code>svn add</code> on an <code>*.o</code> file (and this is usually the case because you would <code>svn add</code> the source files before the project is built) then it will be ignored.</p>