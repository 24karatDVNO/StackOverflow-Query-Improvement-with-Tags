<p>IMHO, it's mostly pure noise. The code assigns an array to a variable and then retrieves key <code>a</code>, which is obviously <code>f</code>. I just uses booleans to generate the intermediate variable names and let PHP cast them to strings.</p>