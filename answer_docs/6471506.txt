<p>The documentation for <code>-enumerateGroupsWithTypes:...</code> and <code>-enumerateAssetsUsingBlock:...</code> doesn't say that those methods execute synchronously. From what you've found, it looks like they do their enumeration on a different thread so that you don't have to wait.</p>