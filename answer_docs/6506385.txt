<p>I would have one machine be a controller for the test.  Have that machine be the Hudson server/slave that is triggered to run the test.  </p>

<p>While, either OS could take on that role, I've generally found Linux to be the easier platform to automate.  The easiest approach may be to just drive a simple terminal session over a socket.</p>