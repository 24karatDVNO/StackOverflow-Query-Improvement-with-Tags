<p>You should not use any try-parse method because checking if a string only contains a-Z chars is not the same as parsing a date or a number.</p>

<p>I think you can use regular expressions to validate the input.</p>