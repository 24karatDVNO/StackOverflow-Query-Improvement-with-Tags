<p>Robert Harvey's first link looks good: make sure the body itself is large enough to allow the <code>&lt;div&gt;</code> to do what you want.</p>

<p>So, <code>height: 100%</code> for your <code>body</code> should help you out. </p>