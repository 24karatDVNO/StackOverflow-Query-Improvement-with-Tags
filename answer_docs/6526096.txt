<p><code>input</code> is declared inside the <code>main()</code> method, so it doesn't exist outside that method. You should declare it as a class variable, outside the <code>main()</code> method, so that <code>intro()</code> has access to it.</p>