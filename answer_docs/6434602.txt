<p>Is there a <code>&lt;set-property&gt;</code> tag in your <code>gwt.xml</code> that restricts the values of the <code>user.agent</code> property? This message is generated by <code>UserAgentAsserter</code> as a sanity-check when the runtime configuration of the module doesn't line up with the <code>user.agent</code> values it was compiled with.</p>