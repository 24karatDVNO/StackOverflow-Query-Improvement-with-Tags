<p>It seems encoded with <a href="http://en.wikipedia.org/wiki/Base64" rel="nofollow">Base64</a>, But not sure. if it is, you can take that long string and convert with the function <a href="http://msdn.microsoft.com/en-us/library/system.convert.frombase64string.aspx" rel="nofollow">Convert.FromBase64</a>. You will obtain a <code>byte[]</code> that you can save as the actual pdf.</p>