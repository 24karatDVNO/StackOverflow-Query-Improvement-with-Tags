<p>It means that o is a reserved word for MySQL, that's why you can't just put o. You cannot use 'o' for specifying a column name, single quotes is for values. Slanted quotes are to specify Column names, this removes the problem of having reserved words. If you want to be sure you should always use `` for tables and column names.</p>