<p>Considering you are using the better-half of a website, the serverside, You need to call a Server-side script like a <code>.php</code> file to execute the DB queries and then echo the result to the Client-side, you can make ajax calls to the script to fetch the data and load it into a <code>Select</code> DOM tag.</p>