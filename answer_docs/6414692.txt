<p>The socket is the default for connecting to a local MySQL server. Otherwise, a TCP connection is used.</p>

<p>If you want to use a TCP connection for a local server, use the following:</p>

<p><code>mysql -h 127.0.0.1 -P 3306</code></p>