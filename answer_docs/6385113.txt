<p>Quote the regexp string, that should fix the error. Haven't checked the expression, but that wasn't the question, right?</p>

<pre><code>var patt1 = '^http\://[a-zA-Z0-9\-\.]+\.[a-zA-Z]{2,3}(/\S*)?$';
</code></pre>