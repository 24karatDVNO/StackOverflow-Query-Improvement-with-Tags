<p>Somewhere, you have an unescaped quote, possibly entering via a PHP variable into the HTML.  You need to call <code>htmlentities($variable, ENT_QUOTES)</code> on all of the variables that will end up inside the HTML.</p>