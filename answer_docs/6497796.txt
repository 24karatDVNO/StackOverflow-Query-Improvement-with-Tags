<p>May be its better to provide an API where the consumer could specify a callback that could be called with relevant data whenever an event occurs. That way the consumer of your component would not be polling for data.</p>