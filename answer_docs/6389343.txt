<p>Your rewriterule doesn't match slashes, so it only takes the last segment of the URL. Add a slash and it will match the entire request path. Or you could just matching against <code>^.*/$</code>, if there isn't some reason not to.</p>