<p>You would have to specifically pass that value into php.  Once a value is sent out the the browser as HTML php has no more control over it.  So when it gets resubmitted PHP only 'knows' whats handed to it.  In this case it 'know' the name of the input and the value of the input.</p>