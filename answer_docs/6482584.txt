<p>(As per comment)</p>

<p>Make sure your class file is named <code>beta_key.rb</code> (with the underscore) if the class is camel-cased as <code>BetaKey</code>, since this is what the autoloader expects.</p>