<p>Because you have two different <code>Fern</code> instances, the fact that one of them changes the value of its <code>x</code> field is not seen by the other. (I am guessing, since you don't give the declaration of <code>x</code>. To get better answers, post only <em>complete</em> code.)</p>