<p>Your format string is <code>%d</code> not <code>%s</code>. <code>Scanf()</code> is looking for an integer, not a string. Your loop keeps going forever because it says <code>while(1)</code> so it never ends regardless.</p>