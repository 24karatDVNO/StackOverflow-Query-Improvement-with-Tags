<p>i had the same problem, then tried it inside a batch request and it worked.
<a href="http://developers.facebook.com/docs/reference/api/batch/" rel="nofollow">http://developers.facebook.com/docs/reference/api/batch/</a></p>

<p>(it's just a workaround though, i think it should work as a single DELETE request as well...) </p>