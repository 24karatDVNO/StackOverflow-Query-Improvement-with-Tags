<p>With only one RichTextBox, you can't. What you have to do is instead of working with one RichTextBox, you could have two. Use one to show the data from database, and use another with editing enabled for the user to submit new data. something like how forum handle posts.</p>