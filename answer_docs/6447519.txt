<p>According to the <a href="http://www.php.net/manual/en/wrappers.compression.php" rel="nofollow">PHP compression streams documentation</a> ZipArchive registers the <code>zip://</code> stream wrapper. While I never worked with <code>zip://</code> streams you could check out if <code>zip.compress</code> works. You can build a test from another example found <a href="http://php.net/manual/en/filters.compression.php" rel="nofollow">here</a>.</p>