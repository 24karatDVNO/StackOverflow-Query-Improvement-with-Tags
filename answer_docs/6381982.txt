<p>Your join will reduce round trip times and the amount of calls to the database by bringing back everything in one request. In a server with very high load you would see a lot of performance gain by using the join.</p>