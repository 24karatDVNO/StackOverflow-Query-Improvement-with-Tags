<p>Yes, <code>mysql_query</code> always returns a resource (unless it returns <code>false</code>), which you have to work on using one of the <code>mysql_</code> functions like <a href="http://www.php.net/manual/en/function.mysql-fetch-assoc.php" rel="nofollow"><code>mysql_fetch_assoc</code></a>.</p>