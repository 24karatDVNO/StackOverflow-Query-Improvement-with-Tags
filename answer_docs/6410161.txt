<p>There is no version "1.0" so you'll actually end up searching forever.</p>

<p>The easiest is probably to checkout the sourcecode, strip out all source-files and drop in the ones from the jar and modify them then.</p>

<p>Anyway you should be using the latest andengine.jar (or sourcecode) for obvious reasons.</p>