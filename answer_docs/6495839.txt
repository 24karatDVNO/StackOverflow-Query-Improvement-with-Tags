<p>Very simply, <em>ALWAYS USE A PARAMETER</em>, in case the parameter is not used, send <code>null</code>, if you have several parameters for other "Modes", encapsulate them, into a single parameter.</p>