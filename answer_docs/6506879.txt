<p>If it's an <code>(500) Internal Server Error</code> then you'll have to check with the server to see what the error is.  Since it's localhost I assume you have access to the server.  That's where there error's going to be.</p>