<p>Assuming "http" is always the first four characters then you simply split the string after "http", <code>UrlEncode</code> that part and then call <code>ToUpper()</code> on it. Then join back together with "http" as your prefix.</p>