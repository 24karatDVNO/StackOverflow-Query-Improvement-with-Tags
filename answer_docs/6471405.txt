<p>Like other libraries and frameworks for client-side web applications, Dojo provides a <strong>wrapper</strong> around the native browser ajax capabilities.  Ultimately it can only do what the browsers will allow. It can, however, make things a little more convenient, in a way similar to that by which php makes assembly language programming more convenient.</p>