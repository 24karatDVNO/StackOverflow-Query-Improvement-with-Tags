<p>If display:block; isn't doing the trick, also set <code>margin:0;</code> in the inline style of all the images. I've seen Gmail &amp; Hotmail add extra vertical space on images that went away after I explicitly set all the margins to 0.</p>