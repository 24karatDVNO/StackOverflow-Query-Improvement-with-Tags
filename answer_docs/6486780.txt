<p>Use the <a href="http://en.wikipedia.org/wiki/Haversine_formula" rel="nofollow">haversine formula</a> on the server to get a rough estimate on distances, and serve only the closest 4 or 5 to a the clients phone, then it can get a more accurate distance using the method <code>distanceFromLocation:</code> on CLLocation with just the few records sent by the server. </p>