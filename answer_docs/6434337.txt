<p>What you could do is make variables for all the fields, and write the contents of the user fields to those variables, and then when the image is taken, or canceled, write all those fields back and release the variables. Basically you temporarily save all the fields, then restore them.</p>