<p>I think you need to add the webapplication name before the <code>/j_spring_security_check</code>
like <code>/WebAppName/j_spring_security_check</code>  that will aply the spring on all what comes after <code>/webAppName</code></p>