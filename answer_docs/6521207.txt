<p>Get the path from using this function <code>Environment.GetFolderPath()</code> with <code>Environment.SpecialFolder</code> enumeration. Delete all files contained in that directory while iterating over them.</p>