<p>A <code>StringBuilder</code> is good idea for building your new string, because you can efficiently append new values to it. As for reading the characters from the input string, a <code>StringReader</code> would be a sufficient choice.</p>