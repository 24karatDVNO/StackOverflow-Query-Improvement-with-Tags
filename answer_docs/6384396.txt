<p>It turns out that you can put <code>SELECT</code> directly into the <code>tmpl-&gt;default.php</code> file. I guess I was wrong somewhere yesterday, today it worked.</p>

<p>I still don't like the idea of putting <code>SELECT</code> into <code>default.php</code>, though (I'd prefer to put it in models, the way it should be).</p>