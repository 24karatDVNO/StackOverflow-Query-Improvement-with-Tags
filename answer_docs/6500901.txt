<p>You're looking for the <code>core.whitespace</code> option (see <code>git config --help</code> for details).</p>

<p>You can set this option like so:</p>

<pre><code>$ git config core.whitespace cr-at-eol
</code></pre>