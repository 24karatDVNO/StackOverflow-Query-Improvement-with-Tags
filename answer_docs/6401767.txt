<p>If you have a browser that supports console.log (Firefox for example) you can do a 'console.log(json)' and look at the structure.<br>
You can access names like that:</p>

<p>json.bible.testament[0].book[0]['@attributes'].name<br>
json.bible.testament[0].book[1]['@attributes'].name<br>
...</p>