<p>If you're on Windows, one solution is to use the ISAM CSV driver in a program and you can then write a query across the tables.</p>

<p>Alternatively, load both into Excel, sort on the ID column and then do a compare on the workbooks.</p>