<p>bind them using <a href="http://api.jquery.com/live/" rel="nofollow">.live()</a>. It will attach the event handler to any element that matches the selector even if it doesn't exist on the page yet.</p>