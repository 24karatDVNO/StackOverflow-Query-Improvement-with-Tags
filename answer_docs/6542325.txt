<p>If you are starting with <code>"display:none"</code>, and then switching the display to blank, what would make it show up? Is there a <code>"display:block"</code> in the CSS? Could you try switching the display to <code>'block'</code> instead of nothing?</p>