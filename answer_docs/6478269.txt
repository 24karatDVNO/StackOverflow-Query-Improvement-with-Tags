<p>Set up a heroku git remote - the General Info of the app on Heroku will list a git path</p>

<p>Set it up by, in your Git directory on your machine:</p>

<pre><code>$ git remote add heroku (THAT URL)
</code></pre>

<p>then git push heroku master will know where to do</p>