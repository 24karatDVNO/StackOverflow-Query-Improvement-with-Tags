<p>Using group by only gives individual items by your comparison function. if the comparison is done by <code>prop</code> it will return the objects with distinct <code>prop</code> only. all you need to do is iterate over them and select only <code>prop</code>.</p>