<p>sleep() is a static method of the 'Thread' class. as you might know static methods are invoked directly by the sysntax</p>

<pre><code>MyClass.myStaticMethod().
</code></pre>

<p>you don't need to instantiate a Thread object to call sleep() method.</p>