<p>Does every processing thread write to the shared object after it has processed just one message? That could create a bottleneck. Try accumulating some temporary results in each thread before writing to the shared object.</p>