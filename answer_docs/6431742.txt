<p>The php variable <code>$products</code> is only set on page load/postback (whatever they call it in php).  you're correct in your original assessment that using <code>append</code> in the <code>success</code> callback was one way to do it.</p>

<p>alternatively, you could use something like <a href="http://api.jquery.com/jquery.tmpl/" rel="nofollow">jquery templates</a> for a slightly cleaner approach.</p>