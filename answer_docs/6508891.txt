<p>It's completely "do-able" as HTML5 supports file reading capabilities.  In addition to that, you can manipulate bits and bytes so you can easily read the tags.  I have done something similar, only that, in my case, i only process the dicom image, processing in full bit-depth (16bit) of course : )</p>