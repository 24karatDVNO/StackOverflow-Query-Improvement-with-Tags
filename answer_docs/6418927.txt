<p>If your parent page and your content page are both on the same domain (which I'm presuming they are) you can simply do:</p>

<pre><code>parent.myFunction();
</code></pre>

<p>from your content page to access <code>myFunction()</code> in your parent page.</p>