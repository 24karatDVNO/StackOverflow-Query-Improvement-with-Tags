<p>f.call mean calling function <code>f</code> with arguments <code>this.values[i]</code>. <code>this</code> in foreach is set.
about calling <code>foreach</code> from <code>toArray</code>, passing function with <code>v</code>, the <code>values[i] in foreach</code> become <code>v in toArray</code>.</p>