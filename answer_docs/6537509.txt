<p>No. Transactions ensure that a set of modifying statements are atomic, namely that either all steps succeed or all steps fail. In case there is only one statement, there is no need to use transactions anymore ..</p>

<p>Anyway, T-SQL does that automaticaly. That's why they called it Transact SQL ...</p>