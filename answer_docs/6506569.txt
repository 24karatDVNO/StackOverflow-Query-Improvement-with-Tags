<p>An important think you need to understand is, when scrolling every time a new cell create. so you need to create a custom cell creation method. using that it only one time create and it maintain all data correctly</p>

<p>inside that method you write the whatever object you need initialize and use. inside the cellrowindexpath method just use the object. (viewwithtag)</p>