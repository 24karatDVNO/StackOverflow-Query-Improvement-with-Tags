<p>either use an <code>INNER JOIN</code>. it will only select rows which exist in both tables. or, if you want to still get the ID, but an empty row otherwise, use the <code>friends.desuser</code> column. it will be set, because you select from <code>friends</code> first (left hand side of the join)</p>