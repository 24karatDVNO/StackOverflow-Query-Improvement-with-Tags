<p>RavenDB generally isn't used for reporting, the normal practice is to flatten your RavenDB data into a table structure (using an index) and export that to SQL Server and use Crystal or SQL Reporting Services.</p>