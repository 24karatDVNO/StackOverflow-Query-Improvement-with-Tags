<p>I realized that I asked two unrelated questions, and want to answer the first.  The easy method is to simply use global variables.  Once the 'ddmenu' selector fires, save the person and item values to global variables for further processing.  Easy peasy.</p>

<p>Now, if I can just figure out how to move the .drop class to line-up under the clicked item.</p>