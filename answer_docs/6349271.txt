<p>all that <code>current = new</code> does is make it so that the variable <code>current</code> points at the thing that <code>new</code> is pointing at. No copying takes place, and the function has no effect on that codepath.</p>