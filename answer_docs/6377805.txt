<p>To avoid the ambiguity with the <code>new</code> operator, it might be more appropriate to say that the variable is "popped" when leaving a function: see <a href="http://en.wikipedia.org/wiki/Stack-based_memory_allocation" rel="nofollow">stack-based memory allocation</a>.The same goes for C.</p>