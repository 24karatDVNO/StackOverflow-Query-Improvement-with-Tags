<h3>It's explicitly implementing an interface's indexer.</h3>
<ul>
<li>Explicit implementation causes the <code>Interface.XYZ</code> syntax</li>
<li>The name of an indexer is <code>this</code>, so it becomes <code>Interface.this</code></li>
</ul>