<p>Try the <code>KeyPress</code> event instead.  This lets you prevent users from typing in a letter that you don't want (by setting <code>e.Handled = true</code>).  You'll still want to use Validating to make sure that they didn't paste some bad data from the clipboard.</p>