<p>You can also use the WebServerAuth plugin so users are only allowed access via basic auth. <a href="http://plone.org/products/webserverauth" rel="nofollow">http://plone.org/products/webserverauth</a></p>

<p>That way you can, just by default, protect everything on the site and not worrying about the plone login forms.</p>