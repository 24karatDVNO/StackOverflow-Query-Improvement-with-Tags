<p>It sounds like you either want a boost multiindex, or a single <code>vector</code> of a <code>struct</code> that has all your data members in it, instead of multiple vectors with a single data type each.</p>