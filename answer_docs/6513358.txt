<p>Can we assume that <code>users.uid</code> is the same as <code>field_data_field_city.entity_id</code>? If that's the case you'll want to look into using <a href="http://dev.mysql.com/doc/refman/5.0/en/join.html" rel="nofollow">MySQL joins</a></p>