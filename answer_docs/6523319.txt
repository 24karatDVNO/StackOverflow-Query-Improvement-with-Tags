<p>It sounds like this is being configured in the <code>&lt;customErrors&gt;</code> node of an upstream *.config file.</p>

<p>You should check the upstream files such as machine.config or ApplicationHost.config.</p>

<p><a href="http://msdn.microsoft.com/en-us/library/ms178685.aspx" rel="nofollow">This MSDN article</a> covers all of the config files in the hierarchy and where they can be found on disk.</p>