<p>There's also:</p>

<pre><code>repoquery --tree-provides
repoquery --tree-requires
repoquery --tree-whatrequires
</code></pre>

<p>...depending on what you want. The latest upstream allows you to output in DOT format too.</p>