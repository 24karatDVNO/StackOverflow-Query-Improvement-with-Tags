<p>AIUI, <code>:include:</code> allows you to (surprise) include the contents of another file, keeping the same indentation level of the block in which the include appears.</p>

<p>It will look for the named file in the current directory, but it's something you can override by means of the <code>--include</code> switch.</p>

<p>If you want an example, <a href="http://google.com/codesearch#search/&amp;q=%5C%3ainclude%5C%3a%20lang%3a%5Eruby%24&amp;type=cs" rel="nofollow">this</a> could prove useful.</p>