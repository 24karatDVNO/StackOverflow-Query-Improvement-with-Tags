<p>If <code>bid_button_logout</code> or <code>bidder_name</code> are unique to the situation where you want no margin, add <code>margin-left:0px;</code> to either of those classes (but make sure they are after <code>.pause a</code> in your css file. If not, use your conditional jQuery to add an inline style, which will bypass the original left margin style.</p>