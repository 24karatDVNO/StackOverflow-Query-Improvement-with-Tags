<p>Use PHP (HTML is not a scripting language). If you don't want to see .php extensions, use the RewriteEngine in htaccess to rewrite certain URLs. Example: you can use RewriteRule to dynamically change the name of page.php to page.html. </p>

<p>You <em>could</em> use Javascript, but it's easier and faster in PHP.</p>

<p>Hope this answered your question.</p>