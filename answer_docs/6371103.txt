<p>Actually what you are looking for is a tab bar with the buttons on the top, instead of the bottom.</p>

<p>You could subclass the <code>UITabBarController</code>, hide the default tabbar buttons and add this view with the buttons with the desired position. </p>