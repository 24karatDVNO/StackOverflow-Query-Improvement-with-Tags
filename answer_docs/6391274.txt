<p>WCF uses the <code>DataContractSerializer</code> which is primarily based upon attributes like: <code>DataContract, DataMember, ServiceContract</code> and so forth. But it also supports <code>SerializableAttribute</code> amongst others. This <a href="http://msdn.microsoft.com/en-us/library/ms731923.aspx" rel="nofollow">http://msdn.microsoft.com/en-us/library/ms731923.aspx</a> document gives you all the insight you need.</p>

<p>So it might be that you don't need to refactor all your existing code but it aks some further investigation and testing ;)</p>