<p>when user wants to exit start another activity and add <code>finish();</code> after <code>super.onCreate();</code> and while starting activity for the intent set flag as     </p>

<pre><code>FLAG_ACTIVITY_CLEAR_TOP
</code></pre>