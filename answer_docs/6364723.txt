<p>If you are using Entity Framework Code First (EF 4.1), mark the <code>PersonDetails</code> property in the <code>Person</code> class as <code>virtual</code>. This will enable EF to populate the data automatically so you can simply do <code>person.PersonDetails</code> to get your list of data</p>