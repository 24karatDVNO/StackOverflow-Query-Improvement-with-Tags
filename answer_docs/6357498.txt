<p>One way around this is on the <code>onPause()</code> method you saved the current time, then using the <code>OnResume()</code> create a new timer starting at the time you stored within the <code>onPause()</code> method.</p>