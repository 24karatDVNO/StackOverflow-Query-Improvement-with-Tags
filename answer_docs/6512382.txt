<p>Get a thread dump by running <code>kill -3 &lt;process id&gt;</code>. This will tell you which threads are hanging around &amp; preventing the java process from exiting. Feel free to post (some of) the thread dump &amp; folks can help you figure out what to do next.</p>