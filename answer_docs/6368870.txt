<p>The button type <code>101</code> is undocumented. It is not something Apple has agreed to maintain so it can change any time. It is incorrect to rely on such stuff. You should consider a <code>custom</code> button with images to be sure that it will not break like it has for you now.</p>