<p>To anyone else whom encounters this problem.</p>

<p>Use the plugin <code>jquery.curvycorners</code> instead. </p>

<ul>
<li>This can handle backgrounds-images behind it rather than just being one colour.</li>
</ul>

<p>Then change the images to background inside a DIV object instead. This will fix the problem in all browsers defaulting to CSS3 properties in modern browsers and applying the script in older ones.</p>

<p>Marvellous</p>