<p><code>2.1</code> will always equal <code>2.1000</code>, since they represent the same value (they just differ in <em>representation</em>, which is lost as soon as PHP parses the number). You'll have to store at least one value as a string <code>'2.1000'</code> to get this sort of "representation comparison".</p>