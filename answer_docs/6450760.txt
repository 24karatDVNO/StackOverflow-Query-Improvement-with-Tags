<p>The code seems hopelessly jumbled, but technically you just need to remove the word <code>static</code>. Then you can call <code>dc.addSoundInfo( id, pSoundInfo )</code> where <code>dc</code> is a <code>DataCenter</code> object.</p>

<p>Cheers &amp; hth.,</p>