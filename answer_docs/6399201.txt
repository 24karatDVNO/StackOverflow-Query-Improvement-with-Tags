<p>If the application gets deleted on iPhone, all its related files are also deleted. Including files, SQLite databases and preferences.</p>

<p>If you want preference to persist after deletion, you will need a server and allow the user to store that information there (in the cloud).</p>