<p>Not sure if this will help you in your mission, but you can use jQuery's attribute selector to get a an element with a particular <code>title</code>:</p>

<pre><code>$('a[title="dynamic title"]');
</code></pre>