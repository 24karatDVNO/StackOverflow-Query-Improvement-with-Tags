<p>You can use the following selector to find the first table in the <code>div</code>. Then just apply your color to it:</p>

<pre><code>jQuery("table:first", "#WebPartWPQ2").css('backgroundColor', myColor);
</code></pre>