<p>As i guessed the tuples are returned as objects. Hence for A-Id 222 the B_mod is set as 2121. I used row_num() function to get distinct row nums for each row and used it as a primary key column to get all the rows. This may not be an exact solution but still it worked :-)</p>