<p><a href="http://www.blackberry.com/developers/docs/4.2.1api/net/rim/device/api/system/Memory.html#getFlashStats%28%29" rel="nofollow">Memory.getFlashStats().getFree()</a> is not that accurate, you can see it when measuring storage of the same object several times - values may be different every time.</p>

<p>The best way to measure object size is to calculate its fields size (what you actually doing).</p>