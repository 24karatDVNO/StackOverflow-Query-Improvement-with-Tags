<p>i would store the connection strings in a table and use Parent Child configurations with property expressions</p>

<p>if you a variable in a package then you will need to modify it manually, not so good </p>