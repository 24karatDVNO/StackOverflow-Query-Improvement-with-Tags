<p>The simplest solution would be to add an appropriate <code>background-color</code>, but that would only work when you need an offset of 0 0, like your example.</p>

<p>The more general case would be to create a pseudo-element and apply the shadow and background color to that. I can describe this in more detail if the first idea doesn't apply to your case.</p>