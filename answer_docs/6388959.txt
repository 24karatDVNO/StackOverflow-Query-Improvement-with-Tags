<p>You can do this easily with the <code>rbreak</code> command. Just specify the exact file you wish to monitor and a regex. So, for all functions in a given file :</p>

<pre><code>rbreak file.c:.
</code></pre>