<p>The onload event isn't triggered from all html elements, so you're <em>forced</em> to wait for window load.  It doesn't matter where you load jQuery since it will have to wait for document to be ready.  That total time required to load jQuery plus the rest of the document will be thet same.</p>