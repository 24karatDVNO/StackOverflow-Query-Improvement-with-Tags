<p>Actually you just tell a adapter listview.</p>

<p>after that in <code>holdar.listview</code> take a event in that event when image is changes put </p>

<pre><code>adaptername.notifydatasetchanged();
</code></pre>