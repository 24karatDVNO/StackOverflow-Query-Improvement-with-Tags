<p>Aside from <code>"</code> and <code>@</code> getting interchanged, this also happens to <code>$</code> becomes libras, <code>|</code> becomes <code>~</code>.  I happen to experience it in word, excel and sqldeveloper.</p>

<p>My solution works only if this thing happen without you getting busy with the control panel keyboard setting.  To bring them back to order, try pressing the left <kbd>ALT</kbd>+<kbd>←</kbd>+<kbd>SHIFT</kbd> keys at the same time.  Remember, this trick only works if you have not altered anything in the control panel keyboard setting.</p>