<p>Without seeing the error it's hard to say what's wrong, but on first glance, if the <code>gpa</code> line ( <code>int gpa = (*cumulativeScore)/(*numberOfCourses);</code> ) isn't throwing an error now, it may in the future. <code>gpa</code> should be a <code>double</code>.</p>