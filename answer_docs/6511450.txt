<p>After many ideas and failed attempts, I discovered that the entry is being added to the <code>content_field_image_cache</code> table before I have the chance to touch it. I have no idea what is inserting it; it's nothing in my code. Anyway, I found that using an <code>UPDATE</code> solved my problem.</p>