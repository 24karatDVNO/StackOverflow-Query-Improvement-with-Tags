<p>Since the first parameter is defined as &lt;<strong>this</strong> Type name> as opposed to the usual  the compiler knows to build it in such a way that the first parameter is automatically handled for you, and thus you only need to worry about those params if any that follow.</p>