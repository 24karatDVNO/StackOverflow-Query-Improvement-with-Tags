<p>You cannot open a new window and send a POST to it. One workaround here would be to send an HTTP post back to the original page, and have that page return an ID of the record where the POST contents are stored; then, do a window.open(), passing that ID in the URL. </p>