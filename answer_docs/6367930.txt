<p>It does one iteration for every object in <code>output.keyset()</code>, in this case <code>keySet</code> is a collection of <em>strings</em> so, for each <em>String</em> key within the collection <code>output.keySet()</code></p>