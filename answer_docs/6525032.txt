<p>This is just a guess, but if you have <a href="http://xdebug.org/" rel="nofollow">xdebug</a> installed, then the default recursion level of <code>var_dump</code> output is 3. This setting is <a href="http://xdebug.org/docs/display" rel="nofollow">xdebug.var_display_max_depth</a>
You are using <code>print_r</code>, but some similar recursive limit could be being reached.</p>