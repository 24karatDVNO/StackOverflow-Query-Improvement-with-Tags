<p>the Return statement also exits the function. Assigning a value to the function name does not. Your function will keep executing until the end, so you could potentially change that value again.</p>