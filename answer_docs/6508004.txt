<p>You would post the user details to a script on <em>your</em> server, that script would then format &amp; forward the details to <a href="https://someurl.com/processPayment" rel="nofollow">https://someurl.com/processPayment</a>, read the response XML, parse and store it, then return some content to the user.</p>

<p>Alternatively, look for a hosted solution offered by someurl.com, otherwise you have large &amp; expensive PCI compliance obligations.</p>