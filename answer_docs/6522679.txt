<p>You could use the .click event in a similar way you used the .hover function:</p>

<pre><code>$('.header').click(function(){
  //change color of this
  //change color of all other .header elements to default color
});
</code></pre>