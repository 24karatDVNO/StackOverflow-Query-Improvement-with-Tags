<p>Add a <strong><code>ValueChanged</code> event handler</strong> to the text box. You may need to set a  global var that is the desired number, so the constructor (or whatever) of the other form can change the text of a label, as I don't think you can change it from a different form.</p>