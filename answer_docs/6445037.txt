<p>It seems that you could make one function per option, then chain them (through Where(...) calls).  That way, you only have three methods instead of Four.  Also, you can then combine the smaller operation in more ways if you ever need to.</p>