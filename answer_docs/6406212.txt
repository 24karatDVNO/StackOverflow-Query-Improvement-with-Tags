<p>The problem is <code>Stream</code> is an abstract class and therefore cannot be constructed as you did with <code>Stream stream = new Stream();</code> change that line to <code>Stream stream;</code> and it should work.</p>