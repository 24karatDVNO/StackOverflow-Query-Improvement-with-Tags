<p>You can extend the <code>Properties</code> class, add a getter and setter for the active namespace, and override the <code>get()</code> method to prepend the active namespace and call the <code>super.get()</code> method.</p>