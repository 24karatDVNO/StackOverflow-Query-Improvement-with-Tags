<p>First things first:</p>

<ul>
<li>Make sure the <code>RowType</code> is actually <code>DataControlRowType.DataRow</code></li>
<li>Make sure <code>e.Row.RowIndex == 0</code></li>
</ul>

<p>Set a breakpoint in the code so you can make sure of those values.</p>

<p>After this, it looks like your code should work.</p>