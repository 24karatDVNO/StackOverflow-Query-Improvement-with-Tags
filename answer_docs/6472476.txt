<p>It is slightly more efficient to use <code>public static readonly string</code> than <code>public static string{get;}</code> as the former eliminates the method call overhead.  On the other hand, I think this would be optimized away by the compiler.</p>