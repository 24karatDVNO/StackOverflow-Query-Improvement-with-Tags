<p>load by defaults use the <code>HTTP</code> method as <code>GET</code>. Looks like the url <code>/return_result</code> doesnot accept <code>GET</code>. If it is your service make sure it supports <code>GET</code>. or use <code>.ajax()</code> method of jQuery to set the <code>type</code> to what ever method it supports.</p>