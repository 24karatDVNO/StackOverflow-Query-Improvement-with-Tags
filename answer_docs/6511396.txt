<p>Asynchronous calls will not "freeze" the browser. If you have already checked that you are executing your AJAX call asynchronously (the default method), you should check the process that converts your JSON responses into divs. This process is not asynchronous, and may block other operations.</p>