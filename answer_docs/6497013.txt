<p>What prevents you from doing the same thing in Java? Have a factory method that you pass some data to, and it returns an implementation of your interface. Since you say implementation is pretty trivial you would have a limited set of concrete classes.</p>