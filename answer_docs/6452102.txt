<p>Unless the JS variable is assigned a static value in its code (in which case, just grep the JS code for what you need), then you will have to actually execute the JS code using a real JS scripting engine, just like a webbrowser would.</p>