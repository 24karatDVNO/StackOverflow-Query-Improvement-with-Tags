<p>You will have to capture the current scroll position with javascript and then store it on a hidden field, then on your action create a javascript to return the user to the same position and print it in the view so that it gets executed, that's how webforms does it.</p>