<p>This answer in <a href="https://stackoverflow.com/questions/1560762/xslt-wont-allow-me-to-use-self-closing-img-and-br-tags/1560815#1560815%5D">another thread</a> doesn't seem to work in my case; even if I specify <code>&lt;xsl:output method="html"...&gt;</code> it still produces <code>&lt;div/&gt;</code> instead of <code>&lt;div&gt;&lt;/div&gt;</code>.</p>

<p>I don't know if my IDE or compiler is broken (IBM Rational Application Developer), but I'm using a work-around of detecting blank nodes and inserting single spaces in them. Less clean, but effective...</p>