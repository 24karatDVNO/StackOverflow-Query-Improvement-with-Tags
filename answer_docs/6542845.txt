<p>I employ a Sequence variable that indicates which step a task is currently in. I then put in an <code>If</code> statement at the PickBranch Action that verifies whether I'm currently at the correct step or not.</p>