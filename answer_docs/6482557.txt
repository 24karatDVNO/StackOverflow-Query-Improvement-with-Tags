<p>As zneak said, the handling is backend specific. The important thing to realize is that each element with the attribute 'name' specified will be sent via the POST/GET, even if it is empty. Thus the way to differentiate between them is simply by their order.</p>