<p>If you don't put an else part and the if condition is not met the program will continue executing its flow:</p>

<pre><code>if (SomeCondition)
{
    // Do something if the condition is not met        
}

// continue executing
</code></pre>