<p>As you define an ID for it, it is presumably (at least it should be), the only element with that ID, and as such you can get it simply by using <code>$("#videoid")</code> and its value with <code>$("#videoid").val()</code></p>