<p>IE is much slower than Chrome/Firefox for all JS work, not only complex ones.</p>

<p>There is no way to speed up IE with just JS code (except if you can optimize scale() function itself).</p>

<p>I think the better way is to use a simpler path for IE users if you can.</p>