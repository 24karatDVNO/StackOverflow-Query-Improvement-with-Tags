<p>I've solved the issue by dropping all of the relevant controls into an update pannel, this seems to allow the animated gif to run without calling the  setTimeout("UpdateImg('MainContent_imgWait','Images/loading.gif');",50); JS method. Working fine now with the server event only firing once.</p>