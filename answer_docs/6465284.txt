<p>It simply states that the backing variable for popOverTableData is _popOverTableData. Normally without this, and just doing @synthesize popOverTableData means that the backing variable is called the same.</p>