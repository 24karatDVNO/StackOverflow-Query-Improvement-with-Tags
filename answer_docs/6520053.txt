<p>Check the source of the web page from browser, there will be a new line. Since in HTML, whitespaces like newline, tabs or spaces are ignored, you can't see them in the browser. You need to use <code>&lt;br /&gt;</code> or <code>&amp;nbsp;</code> etc. to print what you want.</p>