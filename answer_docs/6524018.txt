<p>The file is being read all at once.  There are limits.  Instead of reader.ReaderToEnd(), use reader.ReadLine() and read and process the file one line at a time.  Or if the file doesn't have "lines" read the input file in chunks and save the output chunks as it is being processed.</p>