<p>Try using the <code>indexOf</code> method of javascript's String object in combination with the <code>substr</code> method, i.e.:</p>

<pre><code>$string.substr(0, $string.indexOf('T'));
</code></pre>

<p>I'm more a java/perl coder than a javascript coder, so my syntax might be slightly off, but I hope you get the idea.</p>