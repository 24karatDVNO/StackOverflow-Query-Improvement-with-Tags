<p>If you want to maintain compatibility to old iOS versions, replace the built in background "color" (such as <code>linen</code>) with an UIImageView showing an image of it.</p>

<p>Write a simple app, filling the entire screen with the background, make a screenshot, then place it behind all other UI elements.</p>