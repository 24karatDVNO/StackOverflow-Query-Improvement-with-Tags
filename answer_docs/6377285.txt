<p>I ended up adding a Height Request to the container, just slightly longer than the part of the scrolled window.  This now stops resizing to where the bottom buttons would overlap the components above it.  The problem (before the fix) is shown below:</p>

<p><img src="https://i.stack.imgur.com/Iz4z0.png" alt="Regular Sized Panel"></p>

<p><img src="https://i.stack.imgur.com/u2UiZ.png" alt="Resized with overlap"></p>