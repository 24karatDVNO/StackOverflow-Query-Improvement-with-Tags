<p>If you closed it rather than deleting it (the two are different technically but appear much the same to users) you can restore it from the closed web part gallery.</p>

<p>If it is actually deleted you probably need to export the web part from a working site and adjust the guids in the file - SharePoint blogs aren't as customization-friendly as the other site templates.</p>