<p>Here is the <a href="http://download.oracle.com/javase/tutorial/jdbc/basics/prepared.html" rel="nofollow">official tutorial</a> on using <code>PreparedStatement</code> in JDBC. There are also plenty of others if you search around.</p>

<p>For the record, I must say that I disagree with the claim that the main advantage of a prepared statement is that it can be (though isn't necessarily) sent to the database in advance. The main advantage is parameters.</p>