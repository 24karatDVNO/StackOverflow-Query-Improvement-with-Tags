<p>Eclipse's POM editor has a useful dependency hierarchy view. Another way to test these things is to delete bits of your <code>.m2/repository</code> directory (after backing it up) and seeing if it then also fails to build on your own machine.</p>