<p>You could use a UNION, one query for the first results UNION results for the second query. That should return it the right way: </p>

<p>SELECT * FROM * WHERE <em>=</em></p>

<p>UNION</p>

<p>SELECT * FROM * WHERE <em>=</em></p>

<p>Fill in the *s for your needs.</p>