<p>If it shows "Internet Explorer cannot display the webpage" then it's most likely a server error.</p>

<p>It may not matter but the <code>asp:Button</code>'s should be <code>OnClick</code> (capitalized) for the server side click handler.</p>

<p>Can you confirm that <code>btnPartNumber_Click</code> is being called each time in Visual Studio?</p>