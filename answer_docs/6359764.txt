<p>I know of no way to explicitly refer to a parameter. The way this is usually handled is to give member variables a special prefix such as <code>_</code> or <code>m_</code> so that parameters will never have exactly the same name. The other way is to refer to member variables using this.var.</p>