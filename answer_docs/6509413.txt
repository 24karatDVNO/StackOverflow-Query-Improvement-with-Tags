<pre><code>$("#myTable").wrap("...");
</code></pre>

<p>That will wrap the table in the tag supplied to the <code>wrap</code> function, without altering the <code>table</code> itself.</p>

<p>For more information, see the <a href="http://api.jquery.com/wrap/" rel="nofollow">jQuery API</a> for the <code>wrap</code> function.</p>