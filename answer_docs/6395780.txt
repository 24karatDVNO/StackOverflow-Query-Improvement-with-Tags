<p>We always use a reference to an object and cannot use the object directly, we can only use the reference. <strong>Object instance itself is in memory.</strong></p>

<p>When we create an object, we get a reference. We can create more references:</p>

<pre><code>var obj = {}; // a reference to a new object
var a = obj; // another reference to the object
</code></pre>