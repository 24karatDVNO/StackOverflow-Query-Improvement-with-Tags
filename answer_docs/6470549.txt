<p>When you set the 'disabled' flag on an input field, it prevents that field from being submitted to the web server - as far as PHP is concerned, it doesn't exist.  Use <code>readonly="readonly"</code> instead.</p>