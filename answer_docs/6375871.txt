<p>Most browsers can't display TIFF and afaik no browser can display PCX images, using standard <code>&lt;img&gt;</code> tags.</p>

<p>A solution would be to convert the image to a supported format, either manually or automatically (using php or server-side language).</p>