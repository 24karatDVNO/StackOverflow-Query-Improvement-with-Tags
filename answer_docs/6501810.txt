<p>Look at <a href="http://api.jquery.com/jQuery.getJSON/" rel="nofollow">$.json</a>, it is made specifically for this.  It is an ajax call, that automatically parses the json on return (into an array) to be used in the callback.</p>