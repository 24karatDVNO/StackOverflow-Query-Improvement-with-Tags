<p>A simple method for Delphi would be be to use ParamStr(0) - which has the full path and filename of the exe. Store the exe file's DateTimeStamp at program start. Then periodically check if it has changed...</p>