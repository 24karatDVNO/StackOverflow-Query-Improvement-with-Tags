<p>You can get your home activity to bind to the service in <code>onResume()</code>, and notify the service that it's running. Then in <code>onPause()</code> you can make another call to the service to notify it the activity is no longer in the foreground, and unbind.</p>