<p>Right after the definition of <code>Values2()</code>, there is the line <code>int a,b;</code> which will conflict with <code>int a,b,d;</code> before <code>Values1()</code>, so you can safely remove the line <code>int a,b;</code>.</p>

<p>Besides, to get good results, you may want to set tha values of <code>a</code>, <code>b</code> and <code>c</code> in the start of the main function to get consistent results.</p>