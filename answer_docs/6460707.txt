<p>My suggestion would be to insert the file into a temporary holding table where the date column is a character datatype. Then write a query with the<code>STR_TO_DATE</code> conversion to move the data from the holding table to your final destination.</p>