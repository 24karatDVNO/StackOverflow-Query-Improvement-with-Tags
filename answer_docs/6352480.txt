<p>Are you trying to print the address or the value? From how your code is setup you should be using <code>%d</code>, but for the pointer you would use <code>NSLog("%p", ans);</code>. The <code>%@</code> format is for objects only.</p>