<p>Textmate has a command line helper program called <code>mate</code> which is installed in your path. This line in config says run that program (which runs TextMate), and the <code>-w</code> flag means wait for the file to be closed before carrying on.</p>

<p>For Scite - it may have a command line tool and a flag that lets it behave the same way.</p>