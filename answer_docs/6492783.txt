<p>My bet: you are changing the image of a link which contains a submenu. When you move the mouse over the submenu, you are not longer on the <code>&lt;a&gt;</code>, I think you should change the height of the <code>&lt;a&gt;</code> element to adapt the one of the submenu when it's displayed. Would be better if you give the background image to the <code>&lt;li&gt;</code> instead of the  I guess.</p>