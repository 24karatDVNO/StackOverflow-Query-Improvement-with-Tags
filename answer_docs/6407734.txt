<blockquote>
  <p>Conductor is then being set to the
  address of root which means the while
  loop will never be reached right?</p>
</blockquote>

<p>If <code>root</code> is non<code>0</code> then <code>conductor</code> is also non<code>0</code>, So <code>while()</code> loop will be definitely reached.</p>