<p>Are you declaring it inside a .cu file where the kernel using the surface is declared ?</p>

<p>That statements needs to be parsed by the nvcc compiler to become effective.</p>

<p>If that does not work then post the error log of the compiler.</p>