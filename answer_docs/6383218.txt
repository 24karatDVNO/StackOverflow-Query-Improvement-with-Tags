<p>If they're not somewhere in the application's install directory, then they must be in <code>WINDOWS/system32</code>. (I assume that folder is actually called <code>system64</code> on 64 bits versions of Windows.)</p>