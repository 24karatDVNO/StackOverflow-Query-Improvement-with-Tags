<p>Just manually refresh the page once the log in is done using the header function. Once the cookie is set, it can be accessed only in the next page reload</p>

<p>See the <a href="http://php.net/function.setcookie" rel="nofollow">setcookie documentation</a>.</p>