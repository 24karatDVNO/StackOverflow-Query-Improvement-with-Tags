<p>Looking at the documentation, I don't think there is a way to do this from within <code>np.genfromtxt</code>. Your best bet is probably to read in the data as you are currently doing, and then create a new array that combines the two columns as a later step.</p>