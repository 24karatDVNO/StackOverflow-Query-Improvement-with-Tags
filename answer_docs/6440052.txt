<p>Android does not have a C: drive. You need to put the video file on the device (e.g., copy it to the device's external storage), then supply <code>VideoView</code> with an appropriate path to the file (e.g., using <code>Environment.getExternalStorageDirectory()</code>).</p>