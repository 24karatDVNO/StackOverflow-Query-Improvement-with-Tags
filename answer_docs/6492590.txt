<p>You're trying to include file that does not reside in directories specified in <code>open_basedir</code>.</p>

<p>Either move everything to <code>/home/</code>, or add <code>/usr/local/apache/htdocs/</code> to <code>open_basedir</code>.</p>