<p>You need to make sure the user has authenticated with your application by calling the FB.login() or validate that they have already authenticated by calling FB.getLoginStatus().  Otherwise, me() won't exist as you are experiencing because they haven't authenticated. </p>