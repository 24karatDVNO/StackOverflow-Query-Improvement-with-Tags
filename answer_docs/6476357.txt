<p>Not sure about the <code>background-color</code>, but try for the link:</p>

<pre><code>a:link {
  text-decoration: none;
}
</code></pre>

<p>The selector <code>a</code> is usually too general for overriding link style set by the browser with an <code>a:link</code> selector.</p>