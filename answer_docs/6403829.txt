<p>If this is a linux server it's probably set up to send to the local mail queue. When I had this problem I got it working by adding an <code>MX</code> entry on the DNS server used by the linux servers which pointed to our ISP's mail server.</p>