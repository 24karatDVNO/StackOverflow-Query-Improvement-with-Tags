<p>you can clone an element in jquery with clone(), like
$("#whatever").clone().appendTo("#container")
would clone the element with id 'whatever' and insert it at the end of element 'container' (which could be a <code>&lt;div</code>> for example)</p>