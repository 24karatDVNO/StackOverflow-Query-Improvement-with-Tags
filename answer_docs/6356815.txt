<p>Wrap it in a macro and post process the result (that is, the generated stub).<br>
Use <code>DTE.ExecuteCommand("EditorContextMenus.CodeWindow.Generate.GenerateMethodStub")</code> to activate the tool.</p>

<p>Quite ugly, but it doesn't seem that visual studio externalize any interface to it...</p>

<p>HTH</p>