<p>You want to look up a function called <code>simplexml_load_string()</code> - this will take a text string - or in your case the XML.
You can get the XML String using <code>file_get_contents()</code>. With <code>simplexml_load_string()</code>, you get returned an object which portrays the whole structure of the XML file, you can then traverse this till your heart is content, quick, simple and easy!</p>

<p>You might want to double check that your actual XML syntax is valid &amp; structured in a way which makes it easy to traverse when you parse it to an Object with <code>simplexml_load_string()</code></p>