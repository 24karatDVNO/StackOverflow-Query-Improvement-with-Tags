<p>The <code>webview.loadUrl(theURL);</code> starts the loading of the page, that can take a while. Wait before drawing your webview until after the page is loaded. You can use a <code>setWebViewClient(WebViewClient client)</code> to set up an object to receive notice when the page has finished loading.</p>