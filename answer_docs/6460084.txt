<p>Easiest way would probably be to add the page to your page within a hidden element and select the span using getElementById. Alternately if it is xhtml, you could load it into an xml document and use xpath. And, possibly the hardest, you could parse the string and find the part that matches up to the html you want.</p>

<p>Or, either of these fine examples :)</p>