<p>You might be able to add a custom method into a derived SQLDialect, and use that in a criteria, look into <code>RegisterFunction</code> in NHibernate.Dialect.Dialect which all Dialects inherits.</p>