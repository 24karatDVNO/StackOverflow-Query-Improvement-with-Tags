<p>I can answer the question in a simple string. For this suppose you declare a <code>string s</code> so just write:</p>

<pre><code>s = subString(int startIndex, int endIndex);
//this is because a string is immutable
</code></pre>