<p>Unless the C# team has improved the performance for <code>LINQ</code> and <code>FindAll</code>, the following article seems to suggest that <code>for</code> and <code>foreach</code> would outperform <code>LINQ</code> and <code>FindAll</code> on object enumeration: <a href="http://architecture-blog.rdacorp.com/2009/03/linq-on-objects-performance.html" rel="nofollow">LINQ on Objects Performance</a>. </p>

<p>This artilce was dated back to March 2009, just before this post originally asked.</p>