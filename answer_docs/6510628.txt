<p>The class files are compiled classes and you cannot view them in your IDE.  Your IDE will allow you to import them but just drop off the extension...</p>

<pre><code>import example.some.Thing;
</code></pre>

<p>The .java files are the source files that were most likely used to compile the .class files. I'm sure they are that just for your reference. </p>