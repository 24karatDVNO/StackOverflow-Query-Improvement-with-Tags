<p>You should implement the GetHashCode and Equals methods on SearchItem, so that two different instances can be compared. (By default, two objects are considered equal if they are references to the same instance)</p>