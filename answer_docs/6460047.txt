<p>This is a convenient correction. <code>manager</code> is a pointer, so the <code>-&gt;</code> operator is the correct way to access its members. Nice that Qt Creator has this much built-in intelligence.</p>