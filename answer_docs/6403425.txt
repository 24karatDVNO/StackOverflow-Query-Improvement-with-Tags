<p>You can use the <a href="http://www.cplusplus.com/reference/std/limits/numeric_limits/" rel="nofollow noreferrer">std::numeric_limits</a> template class to get some information regarding the primitive types which have specializations.</p>

<p>Note that on some compilers a <code>long double</code> is larger than the <code>long long</code> although on others a <code>long double</code> is the size of a <code>double</code> (MSVC).</p>