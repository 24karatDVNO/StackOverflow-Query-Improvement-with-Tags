<p>I did not include the javascript that was actually causing the problem (it was in the <code>//do stuff</code>, but the problem I was having was I was looking for the event.target to be just the outsideContainer instead of the <code>ul</code> and <code>li</code>s.</p>