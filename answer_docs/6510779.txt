<p>I would simply do the platform-equivalent version of <code>chdir(data_base_dir);</code> in your program's startup code, then use relative unix-style paths of the form <code>"images/filename"</code>. The last systems where this would not work were MacOS 9, which is completely irrelevant now.</p>