<p>One way would be to loop through the list and use <code>string.Contains</code>, <code>string.StartsWith</code> &amp; <code>string.EndsWith</code>, or a Regex to test if the current item is what you are looking for.  Not particularly efficient but fairly easy to write.</p>