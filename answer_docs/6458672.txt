<p>i will prefer to use, <code>equals()</code> method, instead of comparing the reference variables, it might give proper result in same cases, but one should check if the string is logically equal or not.</p>