<p>You must iterate through the <code>Items</code>.</p>

<p>Refer to <a href="http://msdn.microsoft.com/en-us/library/system.web.ui.webcontrols.checkboxlist.aspx#Introduction" rel="nofollow">CheckBoxList Class</a>.</p>

<p>To determine which items are checked, iterate through the collection and test the <code>Selected</code> property of each item in the list.</p>