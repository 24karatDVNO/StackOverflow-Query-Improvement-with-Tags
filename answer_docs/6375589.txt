<p>You can escape each space with a backslash:</p>

<pre><code>some\ file\ with\ spaces.ext
</code></pre>

<p>That way, each space is passed on <em>quoted</em>, and the shell won't parse the space to mean "this is the end of one argument and the start of another".</p>