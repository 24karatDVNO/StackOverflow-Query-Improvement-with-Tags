<p>You might look up the value using m_soundListMap.find(x).  This returns an iterator.  If the iterator is m_soundListMap.end() then the key wasn't found and you can insert if needed.
No exceptions are thrown.</p>