<p>"Missing plugin" can show either if the plugin isn't loaded (e.g., you typo'd the MIME type in the object tag), or if the plugin crashes during initialization.</p>

<p>Have you tried running with <code>--plugin-startup-dialog</code> to see if your plugin is actually instantiated, and if so debug it?</p>