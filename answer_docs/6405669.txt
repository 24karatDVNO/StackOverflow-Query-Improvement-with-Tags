<p>It IS recursive. Probably the critic is it follows too closely the iterative (loop) version.</p>

<p>Probably then were expecting that, instead of going through the list from beginning to end, you picked the middle city, calculated the distance from the start to that city and from that city to the end and added that. Each half of the distance would be calculated calling the same function.</p>