<p>You can change the <code>getElementsByTagName("div")</code> to get the <code>td</code> elements:</p>

<pre><code>var searchElement=document.getElementById('content_Data').getElementsByTagName("td");
</code></pre>

<p>And, it's better to declare the <code>j</code> variable on the second <code>for</code> :D. That's all you need to change</p>