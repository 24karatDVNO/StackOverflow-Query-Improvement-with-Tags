<p>Based on the return of the function, you could add/remove a class from the element, causing its colors to change.</p>

<pre><code>document.getElementById('FOO').className += 'new-class'
</code></pre>

<p>Alternatively, you can change the style of an element in JavaScript in the following way</p>

<pre><code>document.getElementById('FOO').style = { color: '#FFF', backgroundColor: '#000' };
</code></pre>

<p>Refer to <a href="http://noobflash.com/set-style-dynamically-with-javascript/" rel="nofollow">This reference</a> on the JavaScript names of style options.</p>