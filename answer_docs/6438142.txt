<p>Select a column. In properties you have <code>Hidden</code>. property. Then you can set a condition, for example <code>=Parameters!IsColumnHidden.Value</code>.</p>

<p>If you want to do this from C# code I would send a parameter (like above) to the report saying if column should be hidden.</p>