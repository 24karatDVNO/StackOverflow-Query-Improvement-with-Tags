<p>Have you tried using <code>if($(this).attr('checked'))</code> instead of <code>if ($("input:checked"))</code> ?
<code>attr</code> method is a getter or setter method according to the parameters you use.</p>