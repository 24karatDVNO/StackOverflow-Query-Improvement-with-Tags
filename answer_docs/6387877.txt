<p>This sounds like it could be a job for <a href="http://docs.jquery.com/Plugins/livequery" rel="nofollow">livequery</a>, which allows you to run a function when an element is dynamically added to the DOM.</p>