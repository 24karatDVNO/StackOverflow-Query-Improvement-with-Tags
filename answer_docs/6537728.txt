<p>That's because the selector you're using for setting your <code>'#slider1 li'</code> width is setting them all at the same time (to whatever the last imageWidth was). You need to only set the <code>.parent()</code> width.</p>