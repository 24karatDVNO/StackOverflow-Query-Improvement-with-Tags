<p>In my experience in a self-hosted environment you would (typically) handle this at the machine.config level. In a hosting environment where the host provider doesn't have this already done for you, you should do this at your application level web.config.</p>