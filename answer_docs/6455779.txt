<p>Create a <a href="http://msdn.microsoft.com/en-us/library/system.data.dataview.aspx" rel="nofollow"><code>DataView</code></a> and use that as the <code>DataContext</code> of the <code>DataGrid</code>.  Add the filter and the sort criteria to the <code>DataView</code>.</p>