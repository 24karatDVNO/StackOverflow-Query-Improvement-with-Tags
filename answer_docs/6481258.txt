<p>If you want to share information between modules, attach the information to the module object, and have the other modules rely on that module, and check its property.</p>

<p>If you have existing code, you can assign to <code>window.x</code> to provide a global <code>x</code> while you are cleaning it up.</p>