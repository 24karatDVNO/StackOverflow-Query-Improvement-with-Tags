<p>When you pass <code>animateLeft</code>, it refers to a function. But when you pass <code>animateLeft()</code>, it refers to the <strong>result</strong> of this function, so it executes immediatly when you call <code>animate()</code>.</p>