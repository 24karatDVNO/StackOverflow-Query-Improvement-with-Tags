<p>I think both are the same. Personally I prefer <code>if($var: ... endif;</code>. php is a template language and that syntax leads to extremely well toward it. I actually put all variables in <code>&lt;? ... ?&gt;</code> tags to keep it all clean and concise.   </p>