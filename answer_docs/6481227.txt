<p>You can use the <code>+bundleForClass:</code> class method of <code>NSBundle</code> to get a reference to the bundle for a particular class.</p>

<p>You can then use the <code>‑pathForImageResource:</code> method of <code>NSBundle</code> to get the path to the image and create an <code>NSImage</code> with that path.</p>