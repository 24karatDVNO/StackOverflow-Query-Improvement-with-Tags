<ol>
<li>Make the button a submit button.</li>
<li>Use validate's submitHandler property to change what happens when the form validates.</li>
<li>Let validate's error display do its thing. If you need an alert instead of an error label, you can change how the error displays (check the documentation).</li>
</ol>