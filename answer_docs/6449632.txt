<p>The internal floating point representation of the number <code>2.1000</code> is exactly the same as <code>2.1</code>, so the code can't tell the difference.</p>

<p>We must always be aware of the difference between the <em>internal representation</em> of numeric value, and the representation we use in code or see in output.</p>