<p>If you have the appropriate fonts, a <code>println</code> or setting the string in a JLabel or JTextArea should do the trick. The escaping is only for the compiler.</p>

<p>If you plan to copy-paste the readable strings in source, remember to also choose a suitable file encoding like UTF8.</p>