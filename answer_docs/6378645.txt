<p>Without any details on your table structure and the data in question, the basics would be somethign like:</p>

<pre><code>SELECT ...
FROM ...
WHERE (field1 = $location_option) or (field2 = $category_option)
</code></pre>