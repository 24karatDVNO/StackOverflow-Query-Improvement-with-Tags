<p>It might be that Windows uses backslash, so try "\tmp" instead of "/tmp".</p>

<p>Also if all your files are in the same directory, then you can use find &amp; replace and replace <code>open("</code> with <code>open("c:\\your_directory_here\</code></p>