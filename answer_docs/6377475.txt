<p>I am a bit leery of regular expression, so I'm a bit biased when I say, is it possible to just split the string using the PHP function "explode()", using the 'x' character as your delimiter?</p>

<p>Here is a link to the PHP manual for that function if you are not familiar with it:</p>

<p><a href="http://php.net/manual/en/function.explode.php" rel="nofollow">Explode()</a> </p>