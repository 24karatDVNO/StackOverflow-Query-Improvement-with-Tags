<p>ViewGroup has several <code>removeView*(View)</code> methods. Alternatively you can call <code>setVisibility(View.GONE)</code> to make the widget disappear from the screen without actually deleting it.</p>

<p>Barry</p>