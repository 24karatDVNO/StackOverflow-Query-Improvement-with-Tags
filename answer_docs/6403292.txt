<p>A for loop with linear probing would not be an example of a hash table. </p>

<p>The standard thing to do would be to use the hash classes provided by boost, stl, and many other popular libraries. It will probably not be as fast as a switch, but will be far better than looping.</p>