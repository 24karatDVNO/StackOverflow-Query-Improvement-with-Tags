<p>The script you showed uses a third-party javascript library called <a href="http://jquery.com/" rel="nofollow">jQuery</a>. The syntax: <code>$('blah')</code> is used to pick out all DOM elements matching the CSS selector <code>blah</code>. In this case, the code is selecting all elements like <code>&lt;a class="cell1".../&gt;</code></p>