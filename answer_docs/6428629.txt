<p>You can even use <strong>eq</strong> method of jquery if you want to loop through a list of elements.</p>

<pre><code>var rows = $('tr.classname');
rows.eq(0);//first row
rows.eq(1);//second row
</code></pre>