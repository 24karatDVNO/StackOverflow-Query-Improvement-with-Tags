<p>Would it be feasible to just use a <code>UIPickerView</code>, but clipped to the middle row? You could turn off <code>showsSelectionIndicator</code> to remove the overlay and have the delegate pass back custom row views.</p>