<pre><code>$text = "\n________________________________________".$userName.":".$message."\n________________________________________";
</code></pre>

<p>Use <code>.</code> for concatination</p>

<p><code>a</code> would append to a file, so u want to replace the content you would use <code>w</code></p>