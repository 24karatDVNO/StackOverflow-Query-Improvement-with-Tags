<p>The length of char is 8-bit(one byte), isn't 32-bit(four bytes), so a "char []" which length is 256 can store 256 characters. A string "This" includes 5 characters, please don't forget the end-character "\0" of the string.</p>