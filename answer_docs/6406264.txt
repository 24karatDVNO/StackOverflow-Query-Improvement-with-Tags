<p><code>User</code> is meant to store the credentials of users that will be able to log into and use the system via normal Django auth mechanisms. If your project will use Django's auth then yes, <code>User</code> is the correct model to use.</p>