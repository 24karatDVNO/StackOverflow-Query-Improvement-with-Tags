<p>If your class does not have any instance member and only static methods, you should make it static. </p>

<p>You are in no way not obliged to do so, the code will compile and execute just fine without doing so, but marking it static has some advantages, the most important being you won't be able to create an instance by mistake.</p>