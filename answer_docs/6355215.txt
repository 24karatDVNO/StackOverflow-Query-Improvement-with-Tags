<p>It works perfect. print_r(shuffle($slides))) gives the output of TRUE, since the return value of <code>shuffle</code> is a boolean and not an array.</p>

<p>See the working example here: <a href="http://codepad.org/B5SlcjGf" rel="nofollow">http://codepad.org/B5SlcjGf</a></p>