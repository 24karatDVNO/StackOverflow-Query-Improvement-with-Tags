<p>The Text type just stores a Unicode string - serializing it is just a matter of encoding it in a given codec (eg, UTF-8). If that's all you need to send, just send it in the plain in the body of the response.</p>