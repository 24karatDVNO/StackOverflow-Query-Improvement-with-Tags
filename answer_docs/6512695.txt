<p>When you use DOMDocument on html you load it with <code>$dom-&gt;loadHTMLFile()</code> or <code>$dom-&gt;loadHTML()</code> you should also call <code>libxml_use_internal_errors(true)</code> before hand so that it will not crash because of improperly formatted html.</p>