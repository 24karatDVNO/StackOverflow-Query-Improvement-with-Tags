<pre><code>var no_periods = string.replace('.', '');
var new_string = no_periods.substr(0,2);
</code></pre>

<p>will get rid of all the periods and then just get the first three numbers, is that what you wanted?</p>