<p>It will bring everything in <code>std</code> into scope wherever you put it.  If its at file scope, it counts everywhere in the file, if its in a function, it only applies within the function - so it depends on what you want.  Personally, I don't like declaring <code>using namespace std</code>.</p>