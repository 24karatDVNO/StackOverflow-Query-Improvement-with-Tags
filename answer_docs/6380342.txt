<p>Unfortunately VSTO is only a managed wrapper around the Office Interop libraries, so it has no more hooks than using the Interop libraries. It just makes add-in development much easier with helpers and giving us designer support</p>