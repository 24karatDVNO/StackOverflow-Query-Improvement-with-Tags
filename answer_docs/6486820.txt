<p>As per the <a href="http://developer.yahoo.com/performance/rules.html#expires" rel="nofollow">YSlow recommendations</a> you could configure your web server to add an <code>Expires</code> or a <code>Cache-Control</code> HTTP header to the response which will result in user agents caching the response for the specified duration.</p>