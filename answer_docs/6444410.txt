<p>thats probably because your <code>n-&gt;lastname</code> has a string data type. The <code>strtok</code> returns <code>char*</code> so make sure your data structures corresponds well with what the function returns. </p>