<p>Neural network is one algorithm that have two features: 
1. It can train iterativly on new data
2. It can train on same data iterativly, so error is decreased with each iteration. (back propagation learning)</p>