<p>If you are using separate css files for different browsers than you should just load the browser specific css file. In that case you wouldn't see much savings by combining them. In general though, you should try for css that works across browsers. </p>