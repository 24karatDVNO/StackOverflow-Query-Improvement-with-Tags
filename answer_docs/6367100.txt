<p>From looking at your sample code, it seems like you're storing them properly(ish), and you've managed an exhaustive job fetching them in a really narrow scope, so make <code>first</code> and <code>middle</code> more globalish and since you have two strings available, put them in an array.</p>

<p>Though I must say if this is your actual code it probably won't work the way you want this whole offline thing to work.</p>