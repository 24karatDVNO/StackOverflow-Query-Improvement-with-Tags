<p>You just need to put it inside single quotes.</p>

<p>It also works on comparission blocks like:</p>

<pre><code>{if $variable == 'hyphenated-value'} it works! {/if}
</code></pre>

<p>instead of</p>

<pre><code>{if $variable == hyphenated-value} do not work! {/if}
</code></pre>