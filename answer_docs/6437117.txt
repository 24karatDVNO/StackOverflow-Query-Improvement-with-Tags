<p>NewsFox puts article content into a frame with URL <code>about:blank</code>. You can try creating a GreaseMonkey script that will run on <code>about:blank</code> pages but I am all but sure that it will work.</p>