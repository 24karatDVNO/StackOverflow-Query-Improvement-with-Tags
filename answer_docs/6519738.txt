<p>Your question is incredible vague but you are probably looking for about the <a href="https://developer.mozilla.org/en/HTML/Element/xmp" rel="nofollow"><code>&lt;XMP&gt;&lt;/XMP&gt;</code></a> tag set. However, such element has been deprecated for quite long: you can use it for some quick testing but it has no place in a live site.</p>

<p>The only serious approach is to use a server-side language that takes care of escaping for you.</p>