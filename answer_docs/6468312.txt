<p>if you on C# 4, you could <code>ConcurrentQueue</code> class which is thread safe for your queue</p>

<p>or have a look other Concurrent* classes in C4. Parallel extensions are quite handy as well, but oyu will need to tune it to yr need</p>