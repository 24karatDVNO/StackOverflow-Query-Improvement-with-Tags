<p>It sounds like this would easiest be achieved by using a table (although this is a swearword in these tableless-times). Not knowing what the elements is used for I don't know if this is semantically correct.</p>

<p>You could also try experimenting with display:table-cell etc. But that's less compatible with older browsers...</p>