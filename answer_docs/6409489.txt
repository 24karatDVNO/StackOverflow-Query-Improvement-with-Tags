<p>You can do two things:
1. Add your image view and labels directly to <code>scrollView</code>
2. also alloc your label and ImageView before adding them to your <code>UIView</code>(and don't forgot to release them when requirement is finish, in respective scope)</p>