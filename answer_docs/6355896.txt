<p>You can use the <code>eval</code> function to evaluate an expression in a string:</p>

<p><code>var evaluated = eval(s);</code></p>

<p><code>alert(evaluated)</code> will then alert <code>1000</code>.</p>