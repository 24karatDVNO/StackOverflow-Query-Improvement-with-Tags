<p>Dom manipulations can cause the browser to freeze. Try to return the div that you want to update rather than just the data so that all you are doing is replacing the current node. The idea is to minimalize the dom interactions.</p>