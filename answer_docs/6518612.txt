<pre><code>if($("#elementId").val().indexOf("airport") != -1) {
    //Contains "airport"
}
</code></pre>

<p>The JavaScript <code>indexOf</code> function returns the index of the first occurence of the specified string. If the string is not found, it returns <code>-1</code>.</p>