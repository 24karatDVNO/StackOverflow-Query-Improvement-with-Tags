<p>But if you want to remove somekind of element based on some order, try using the <a href="http://msdn.microsoft.com/en-us/library/f7fta44c.aspx" rel="nofollow">SortedDictionary</a>. Then you can remove the last key by using <code>dict.Remove(dict.Keys.Last())</code>, as noted by others.</p>