<p>My reason to store total price is performance. I already have contract class, which contain Orders (and also has TotalPrice property). When I do not store total price, and want select list of contracts (to grid for example), it then must always read all orders and it's lines.    </p>