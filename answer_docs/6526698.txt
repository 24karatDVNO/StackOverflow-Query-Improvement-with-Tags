<p>It might be trying to read an <code>int</code>, but then it notices there is no int in the data stream, so it returns immediately. If it doesn't consume the input stream, then it'll repeatedly do that.</p>