<p>It depends on your application needs. If you are doing an image gallery and want to tag uploaded images, you may want to add the data for the images to the database. For this, you may need to put in the database the contents of <code>$_FILES['image']['name']</code>, then a SQL injection it's possible if you don't escape its value.</p>