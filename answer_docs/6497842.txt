<p>You could always combine regexes into one, just by (regex1)|(regex2)|(regex3)... (If this is what you mean by "combine into one")</p>

<p>The question is whether you should, surely? Regexes are generally pretty hard to read as it is, and anything you can do to make them easier to understand and to modify in future is to be encouraged.</p>