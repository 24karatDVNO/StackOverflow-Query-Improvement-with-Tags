<p><code>$this-&gt;agree)</code> if you're going to use it in other functions of the class, <code>$agree</code> if using it within the current scope, meaning inside the function making it a local variable only.</p>