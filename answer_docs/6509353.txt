<p>Sounds like a problem with MySQL itself starting up. You may have to kill the <code>mysqld</code> process if nothing else gets it into a started or stopped state, and then investigate the <code>.err</code> logs in your data directory.</p>