<p>A function pointer would create a way to substitute another implementation.  Furthermore, it's transparent to callers (unless they're doing something really stupid like calling <code>sizeof(funcname)</code>).</p>