<p>Calling a .NET class that is exposed as COM requires marshaling of data. AppDomains separates data access. When you call a COM component (regardless of it's being a .NET app) you have to make it sand-boxed. In .NET, sand-boxing is using different AppDomain.</p>