<p>It does not exist, you should write your own code/command/script to do it. Because all Google Chrome Helper processes are distinct processes, maybe you can write a script which computes all Chrome processes (helper, plug-in hosts etc.) using parent process id.</p>