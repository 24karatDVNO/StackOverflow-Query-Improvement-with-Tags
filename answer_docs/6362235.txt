<p>Your process seems fine.</p>

<p>You can set the <code>ConnectionString</code> property to whatever you want; you should use the <code>ConnectionStringBuilder</code> class to safely build the connection strings.</p>