<p>It's the logical OR operator, but it only evaluates the right-hand expression if the left-hand expression is false.</p>

<p>If the left-hand expression is <code>true</code> then it knows that the whole expression evaluates to true, so it doesn't bother evaluating (or executing) the right-hand expression.</p>