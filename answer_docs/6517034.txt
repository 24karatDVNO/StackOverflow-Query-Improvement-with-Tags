<p>You haven't posted the code that <em>isn't</em> working, which is the important part, so this may be what you have already:</p>

<pre><code>If shpBar.Top &gt; Me.Height Then
    shpBar.Top -= 50
Else
    shpBar.Top = Me.Height
End If
</code></pre>

<p>Does that work?</p>