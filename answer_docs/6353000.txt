<p>Be careful when passing this function a string value with a leading "0". If you give it "042" then, it will treat it as BASE 8 - 9 and convert it to decimal value, which is by default base.</p>

<p>Please go through <a href="http://php.net/manual/en/function.intval.php" rel="nofollow">this</a></p>