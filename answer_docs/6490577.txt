<p>An index on either <code>Entries (diaryNo)</code> or <code>Entries (diaryNo,date)</code>, especially if you are selecting <code>Entries</code> based on <code>diaryNo</code> frequently. i.e. <code>diaryNo=?</code> appears in most of the select queries.</p>