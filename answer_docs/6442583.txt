<p>Don't use custom code if you can help it.   Either copy over the tpl to <code>custom/modules/[yourmodule]/tpls/[yourtemplate]</code> and code the bitton in there  or use an after_ui_frame hook and some js to insert you custom button into the DOM.</p>