<p>It appears you forgot the <code>data-val="true"</code> attribute in your <code>input type="file"</code> tag.
<code>data-val="true"</code> is required for the unobtrusive validation script to even look at the other attributes. Without it the script completely ignores the input.</p>