<p>You can use a separate Array/List <code>var keyList = new List&lt;int&gt;();</code> to store the keys in the order you want, that way you can iterate through the <code>keyList</code> and do a lookup for the value in the original Dictionary.</p>