<p>Turs out both functions <code>system()</code> &amp; <code>exec()</code> actually work. The generated rake output when using --verbose isn't taken into consideration though. That's why I was confused. If anyone has more extensive knowledge on the distinction, do share :)</p>