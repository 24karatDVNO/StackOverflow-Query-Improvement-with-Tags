<p>You can find the official API documentation within the API documentation of the Zend Framework (Because its part of it): <a href="http://framework.zend.com/apidoc/core/" rel="nofollow">http://framework.zend.com/apidoc/core/</a> (in package <code>Zend_Gdata</code>)</p>

<p>As a sidenote: The ZF only implements accessor methods (<code>get*()</code> and <code>set*()</code>) instead of public properties.</p>