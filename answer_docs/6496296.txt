<p>In PHP you are able to receive the last id inserted by using <code>mysql_insert_id()</code> (see <a href="http://www.php.net/manual/en/function.mysql-insert-id.php" rel="nofollow">here</a>) and further <code>mail()</code></p>