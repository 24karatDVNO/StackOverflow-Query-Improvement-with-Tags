<p>it's not an estetic matter, but it's related to parameters for routing.</p>

<p>you can use <code>/articles/:id</code> and then refer to the owner (User) inside controller.</p>

<p>in the second case <code>/users/:user_id/articles/:id</code> will pass 2 params to controller.</p>