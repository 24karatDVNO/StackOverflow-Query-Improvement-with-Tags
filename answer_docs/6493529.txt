<p>This seems pretty easy to test using the <a href="http://msdn.microsoft.com/en-us/library/system.diagnostics.stopwatch.aspx" rel="nofollow"><code>Stopwatch</code></a> class. Your best bet is to test, my gut feeling says PNG, as jpeg would need to be decompressed. EDIT: So does PNG, so testing is definitely the way to go (i was confusing PNG with BMP).</p>