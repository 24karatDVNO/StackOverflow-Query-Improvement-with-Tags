<p>The documentation is telling you to supply the executable file's name to <code>camldebug</code>. You can do this by <code>M-x camldebug</code>, then press enter, and then type in the path of the executable when emacs prompts you for it.</p>