<p>You can use <code>System.Threading.Interlocked.Increment()</code> method. That's will be right choice in your case. See <a href="http://msdn.microsoft.com/en-us/library/system.threading.interlocked.aspx" rel="nofollow">Interlocked class</a> for more info.</p>