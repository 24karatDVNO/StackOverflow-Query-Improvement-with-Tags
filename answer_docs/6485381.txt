<p>I use:</p>

<pre><code>$db['default']['hostname'] = "localhost"; 
</code></pre>

<p>on my local machine, it doesn't really have to be an IP address, only something that your machine can resolve to an IP address..</p>