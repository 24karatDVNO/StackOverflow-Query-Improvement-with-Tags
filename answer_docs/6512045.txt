<p>Just use the built-in Processing classes and methods:</p>

<pre><code>PVector x = new PVector(random(width), random(height));   
PVector y = new PVector(random(width), random(height));
System.out.println(getEuclidianDistance(x, y))
</code></pre>