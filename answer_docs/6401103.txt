<p>this looks fine to me,
the only thing i might have changed, is either give ID's to the editors (Editor1,Editor2) paragraphs, and reference them directry <code>$("#Editor1")...</code>
or <code>$(this).closest('p')...</code></p>