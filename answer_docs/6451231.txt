<p>So i've decided to go down the callback route.</p>

<p>I'll make a call to execute JS code in Obj-C then do nothing in the browser. The next piece of code will be executed in the callback function that is called from Objective-c one all it's work is done.</p>