<p><code>params</code> as it will give you the ability to pass in an array. It doesn't seem like you would want to pass in a list because that would give you extra functionality on the parameter that doesn't make sense. Like <code>Add</code> for example.</p>