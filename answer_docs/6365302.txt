<p><code>this</code> is a HTML element.<br>
<code>$(this)</code> is a jQuery object that encapsulates the HTML element.</p>

<p>Use <code>$(this).text()</code> to retrieve the element's inner text.</p>

<p>I suggest you refer to the <a href="http://api.jquery.com/" rel="nofollow">jQuery API documentation</a> for further information.</p>