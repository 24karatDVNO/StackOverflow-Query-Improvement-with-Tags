<p>As far as I know you have to include it in your <code>Gemfile</code>, or include it prior to using <code>bundler</code>. If you include it after you get an error. There may be a way to turn that off but I can't find it.</p>