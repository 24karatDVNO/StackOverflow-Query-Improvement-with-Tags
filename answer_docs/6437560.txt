<p>You could change the prototype of <code>void Menu()</code> to <code>void Menu(void)</code>. This way the compiler can check whether the function is called correctly or not. It is always good if errors can already be found at compile time.</p>