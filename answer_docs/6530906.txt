<p>from (I think)C# 3.0 onwards you could do:</p>

<pre><code>var _index = new IndexViewModel { 
                   PageMeta = new PageMeta(), 
                   Que = new Q(), 
                   Test = new T()
};
</code></pre>