<p>Your code searches the binary tree (and assumes its elements are sorted) for <code>obj</code>, and if it recursively finds it, deletes it and returns <code>1</code>. If it doesn't find it, it will return <code>0</code>.</p>