<p>Those 'tags' just allow you to enter bare HTML within PHP code without having to use quotations etc. </p>

<p>It's assigning anything between those 2 tags to the variable: <code>$GLOBALS['TEMPLATE']['extra_head']</code></p>