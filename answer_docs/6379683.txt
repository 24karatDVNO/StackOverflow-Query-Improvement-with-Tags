<p>You're setting <code>age</code> in your <code>Student</code> class, but the parent is the one declaring <code>age</code> and they share the same variable - therefore, it makes sense that the value was modified. Overriden methods would be different, however.</p>