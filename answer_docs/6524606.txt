<p>Where are experiencing a problem? Posting the code you've got so far would help. jQuery offers the <code>getJSON()</code> function you need, and you can then parse the resulting string back into a usable JSON object with <code>parseJSON()</code>.</p>