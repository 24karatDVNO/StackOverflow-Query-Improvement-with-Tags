<p>Change <code>if($httpposi === true) {</code> to <code>if($httpposi) {</code> or <code>if($httpposi == true) {</code>, since <code>$httpposi = 4</code> isn't <code>===</code> to <code>true</code>.</p>