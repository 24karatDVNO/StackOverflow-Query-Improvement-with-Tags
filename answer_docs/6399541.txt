<p>you should read them in (say from a file) and use the <code>XamlReader</code> to create concrete instances of them. you can store them in an <code>ObservableCollection</code>, and then bind said collection to an <code>ItemsControl</code> where you have specified the panel as a <code>Canvas</code>.</p>