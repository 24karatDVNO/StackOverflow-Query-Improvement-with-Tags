<p>To validate the username, you should use a <code>NSCharacterSet</code> which only contains the allowed characters and then remove everything from the string which isn't in the set. Web address validating... what is a valid web address?</p>