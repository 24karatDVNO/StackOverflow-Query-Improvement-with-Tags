<p>The default PostgreSQL installation prevents accessing the database in very rigorous ways via pg_hba.conf file. It is up to the system administrator how the database will be exposed to the outside world. Once it is configured, it is up to the application to prevent DB "attacks".</p>