<p>Keep in mind that you don't need the socket library to access the network, you can do with open() read() and write().  So you probably need some kind of sandbox, not just limitations on what's allowed in code.</p>