<p>It is a sequence of 128 bits, encoded as a lower-case hex string.</p>

<p>If you are talking about a Facebook API key, there is no deeper meaning to decode from the bits. The keys are created at random by Facebook and assigned to a particular application to identify it. Each application gets a different set of random bits for its API key.</p>