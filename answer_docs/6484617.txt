<p>If the <code>&lt;iframe&gt;</code> tag is added from JavaScript code, then the site would pass the validation.<br>
I am not sure, too, that when using the Google Maps JavaScript is necessary to use that HTML tag.</p>