<p>There is no security reason. Usually something like this is used to track, where a visitor comes from. <code>redirect</code> will evaluate the <code>id</code>-parameter, that usually specifies one concrete (registered) referer and then redirect to the target page.</p>