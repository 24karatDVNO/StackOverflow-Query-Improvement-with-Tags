<p><code>Anonymous type</code> are class type that are derived directly from <code>object</code>.</p>

<p>You can return it from method as <code>object</code> as return type.
Have a look at <a href="http://msdn.microsoft.com/en-us/library/bb397696.aspx" rel="nofollow">this</a>.</p>