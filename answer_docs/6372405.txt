<p>Unless you can modify all of the SQL inserting/updating data into this table then the only solution I can think of is some kind of daemon or cronjob that searches the table for out of sync rows.</p>