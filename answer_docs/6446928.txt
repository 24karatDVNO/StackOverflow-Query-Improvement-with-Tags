<p>It's not that hard. This is the algorithm:</p>

<pre><code>1. Find all indexes in main starting with &lt;.
2. If none are found, go to 5.
3. Replace every index with the according section (inserted unpacked).
4. Go to 1.
5. Done.
</code></pre>