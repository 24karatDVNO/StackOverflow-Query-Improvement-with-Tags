<p>giv the body an <code>position: relative</code> and the div a <code>position: absolute</code></p>

<p>This way the div will can be positioned anywhere relative to the body, using top and left properties.</p>

<p>In cases like these it makes more sense to use the css in the head or external file.</p>