<p>Looks like the problem was in <code>List&lt;Visual&gt;</code>. After converting to <code>VisualCollection</code> and removing <code>AddVisualChild()</code> and <code>AddLogicalChild()</code> calls problem was solved.</p>