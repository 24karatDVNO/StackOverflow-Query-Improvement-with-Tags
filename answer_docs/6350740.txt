<p>I think it would just be simpler to shuffle one <code>List&lt;string&gt; names</code>, create a copy, shift the copy one item down (make last item become first) and match names by position. You ensure no duplicates in one pass.</p>