<p>Instead of using a GET parameter in the URL why not set some data to the <a href="http://www.php.net/manual/en/session.examples.basic.php" rel="nofollow">session</a>?  You can then redirect as normal but the data isn't visible to the user, it's stored on the server side.</p>