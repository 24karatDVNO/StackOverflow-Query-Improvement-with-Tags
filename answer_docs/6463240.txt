<pre><code>.TB_overlayBG {
    background-color:#000;
    filter:alpha(opacity=75);
    -moz-opacity: 0.75;
    opacity: 0.75;
}
</code></pre>

<p>The "darkening of the screen" is caused by a black overlay with 75% opacity.
Change <code>background-color</code> to <code>transparent</code> if you do not want this.</p>