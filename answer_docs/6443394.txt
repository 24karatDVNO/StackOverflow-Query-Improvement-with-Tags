<p>Java dates, and most computer implementation of dates, are stored as milliseconds of seconds starting from what's called the epoch. That is 1 January, 1970. Java date is represented as milliseconds since epoch. and 0 milliseconds since epoch is the epoch itself. And that's why you get that value.</p>