<p>If the alternate equals method is just for this particular set, I'd suggested using TreeSet (or another SortedSet) instead of HashSet so that you can use the <code>TreeSet(Comparator)</code> constructor.</p>