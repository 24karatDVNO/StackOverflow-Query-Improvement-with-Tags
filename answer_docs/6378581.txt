<p>str1 is a class variable as static is the modifier to it. static means variable belongs to a class not to the instance.
str variable does not have static modifier which makes it member or instance variable.
each instance of a class will have its own copy of member variable</p>