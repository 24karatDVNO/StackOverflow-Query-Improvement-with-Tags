<p><code>fooBar</code> is a reference to an object of type <code>foobar</code>, and such objects do not have a field <code>name</code>. Simple as that. And since it’s an anonymous type, the only way to reference that field is through its <code>this</code> reference.</p>