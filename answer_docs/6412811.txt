<p><a href="http://msdn.microsoft.com/en-us/library/bsc2ak47.aspx" rel="nofollow"><code>Equals</code></a>:</p>

<blockquote>
  <p>Determines whether the specified Object is equal to the current Object.</p>
</blockquote>

<p>Since these two instances <em>are</em> different <code>object</code> instances, <code>false</code> is the correct result.</p>

<p>For an image to truly be compared, you'd need to check byte-for-byte, AFAIK.</p>