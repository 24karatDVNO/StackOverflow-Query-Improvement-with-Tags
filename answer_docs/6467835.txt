<ol>
<li>Avoid bottlenecks in your app design - see <a href="http://developer.yahoo.com/performance/rules.html" rel="nofollow">these tips from Yahoo</a>.</li>
<li>Run the web server, application server and database server on seperate hardware clusters.</li>
<li>If the performance isn't good enough profile to see what is slowing it down; if there is a bottleneck in the code fix it, otherwise add hardware until it is fast enough.</li>
</ol>