<p>You can potentially change the <code>DateTime</code> to Nullable, <code>DateTime?</code> or check the strings you're passing in with <code>string.IsNullOrEmpty()</code> and add whatever logic you think's best to handle null values.</p>